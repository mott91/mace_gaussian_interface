---
phase: 06-extract-gaussian-i-o-zmq-server
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - gaussian/parser.py
  - gaussian/fchk.py
autonomous: true
requirements:
  - STRUCT-04

must_haves:
  truths:
    - "GaussianLogParser and parse_gaussian_log are importable from gaussian.parser"
    - "convert_chk_to_fchk, parse_fchk_section, extract_modes_from_fchk, get_fchk_from_chk are importable from gaussian.fchk"
    - "gaussian_parser.py and fchk_parser.py top-level files still exist (deletion deferred to plan 05)"
  artifacts:
    - path: "gaussian/parser.py"
      provides: "GaussianLogParser class and parse_gaussian_log function"
      contains: "class GaussianLogParser"
    - path: "gaussian/fchk.py"
      provides: "FCHK parsing functions"
      contains: "def convert_chk_to_fchk"
  key_links:
    - from: "gaussian/parser.py"
      to: "utils.exceptions"
      via: "from utils.exceptions import GaussianParseError"
      pattern: "from utils\\.exceptions import GaussianParseError"
    - from: "gaussian/fchk.py"
      to: "utils.units"
      via: "from utils.units import BOHR_TO_ANGSTROM"
      pattern: "from utils\\.units import"
---

<objective>
Move gaussian_parser.py and fchk_parser.py wholesale into the gaussian/ package as gaussian/parser.py and gaussian/fchk.py. Bodies are unchanged — only the file locations change.

Purpose: Relocate the two existing top-level parser modules into the gaussian/ package so all Gaussian-related code is collocated. Old top-level files are kept intact for now; callers are updated in plan 05.
Output: gaussian/parser.py and gaussian/fchk.py with identical content to their originals (except module-level docstrings noting the new location).
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@gaussian_parser.py
@fchk_parser.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gaussian/parser.py from gaussian_parser.py</name>
  <files>gaussian/parser.py</files>
  <action>
Create gaussian/parser.py as a verbatim copy of gaussian_parser.py. No behavioral changes.

The file should be an exact copy of gaussian_parser.py. The internal imports inside the file (e.g., `from utils.exceptions import GaussianParseError`) already use the correct paths and do not need to change.

Do NOT delete gaussian_parser.py yet — that happens in plan 05 when callers are updated.
Do NOT update any callers yet.
  </action>
  <verify>
python -c "
from gaussian.parser import GaussianLogParser, parse_gaussian_log
import inspect
# verify class has expected parsing methods
assert hasattr(GaussianLogParser, 'parse_harmonic_frequencies')
assert hasattr(GaussianLogParser, 'parse_anharmonic_frequencies')
print('gaussian.parser imports OK')
"
  </verify>
  <done>gaussian/parser.py is importable; GaussianLogParser class and parse_gaussian_log function are accessible; GaussianLogParser has parse_harmonic_frequencies and parse_anharmonic_frequencies methods.</done>
</task>

<task type="auto">
  <name>Task 2: Create gaussian/fchk.py from fchk_parser.py</name>
  <files>gaussian/fchk.py</files>
  <action>
Create gaussian/fchk.py as a verbatim copy of fchk_parser.py. No behavioral changes.

The file should be an exact copy of fchk_parser.py. The internal imports inside the file (e.g., `from utils.units import BOHR_TO_ANGSTROM`) already use the correct paths and do not need to change.

Do NOT delete fchk_parser.py yet — that happens in plan 05 when callers are updated.
Do NOT update any callers yet.
  </action>
  <verify>
python -c "
from gaussian.fchk import convert_chk_to_fchk, parse_fchk_section, extract_modes_from_fchk, get_fchk_from_chk
print('gaussian.fchk imports OK')
"
  </verify>
  <done>gaussian/fchk.py is importable; convert_chk_to_fchk, parse_fchk_section, extract_modes_from_fchk, and get_fchk_from_chk are all accessible.</done>
</task>

</tasks>

<verification>
Run existing parser and mode-matching tests to confirm the new modules work identically to the originals:
```bash
cd /home/mot/mace_gaussian && python -m pytest tests/test_gaussian_parser.py tests/test_fchk_parser.py tests/test_mode_matching.py -x -q 2>&1 | tail -20
```
Tests import from the old top-level paths which still exist, so they must pass unchanged.

Also verify new module imports work independently:
```bash
python -c "from gaussian.parser import GaussianLogParser; from gaussian.fchk import extract_modes_from_fchk; print('OK')"
```
</verification>

<success_criteria>
- gaussian/parser.py is importable and exports GaussianLogParser, parse_gaussian_log
- gaussian/fchk.py is importable and exports convert_chk_to_fchk, parse_fchk_section, extract_modes_from_fchk, get_fchk_from_chk
- Existing tests pass unchanged (gaussian_parser.py and fchk_parser.py still present)
</success_criteria>

<output>
After completion, create `.planning/phases/06-extract-gaussian-i-o-zmq-server/06-02-SUMMARY.md`
</output>
