---
phase: 01-testing-infrastructure-characterization
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_regression.py
autonomous: true

must_haves:
  truths:
    - "Water reference outputs (results.json) are committed and validated against expected values"
    - "CH4 reference outputs (results.json) are committed and validated against expected values"
    - "Coverage report runs and shows coverage for core modules (gaussian_parser, fchk_parser, mode_matching)"
    - "Test markers allow separating unit tests from integration tests: pytest -m 'not gpu and not gaussian' runs only unit tests"
  artifacts:
    - path: "tests/test_regression.py"
      provides: "Reference output validation tests for water and CH4, coverage smoke test, marker filtering test"
      min_lines: 60
  key_links:
    - from: "tests/test_regression.py"
      to: "tests/fixtures/water/results.json"
      via: "json.load of fixture file"
      pattern: "json\\.load.*results\\.json"
    - from: "tests/test_regression.py"
      to: "tests/fixtures/CH4_ase/results.json"
      via: "json.load of fixture file"
      pattern: "json\\.load.*results\\.json"
---

<objective>
Write reference output regression tests that validate committed results.json files for water and CH4, and verify the coverage reporting infrastructure works end-to-end.

Purpose: Covers requirements TEST-05 (water reference outputs committed as regression baseline), TEST-06 (CH4 reference outputs committed as regression baseline), TEST-07 (test markers separate unit from integration tests -- verified by test), and TEST-09 (pytest-cov configured and reports coverage). Also validates REPR-04 indirectly (uv.lock committed in plan 01).

Output: tests/test_regression.py with passing tests that validate reference data and verify infrastructure.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testing-infrastructure-characterization/01-RESEARCH.md
@.planning/phases/01-testing-infrastructure-characterization/01-01-SUMMARY.md
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write reference output regression tests</name>
  <files>tests/test_regression.py</files>
  <action>
Create `tests/test_regression.py`. Import: `json`, `pytest`, `from pathlib import Path`.

This test file validates the committed results.json reference outputs for water and CH4. These JSON files were generated by actual pipeline runs and serve as regression baselines. The tests verify the structure and key values are present and reasonable, ensuring that if parsers change behavior, we detect it.

**Test 1: test_water_reference_output_structure(water_results_json)**
- Load `water_results_json` as JSON (it's a Path from the conftest fixture)
- Assert it's a dict
- Assert it contains expected top-level keys. Read the actual results.json first to determine keys -- likely includes keys like `harmonic_frequencies`, `anharmonic_frequencies`, `overtones`, `combination_bands`, or similar. Adjust assertions to match actual structure.
- Assert `len(data["harmonic_frequencies"]) == 3` (or whatever the key is for water's 3 modes)

**Test 2: test_water_reference_output_values(water_results_json)**
- Load JSON
- Verify specific frequency values from the harmonic section match known water frequencies (within tolerance)
- Water B3LYP/6-31G(d,p) harmonic frequencies: ~1665, ~3799, ~3912 cm-1
- NOTE: This results.json is from the ML calculation (mace_mp_espaloma), not DFT. The ML frequencies will differ from DFT. Check the actual values in the JSON and hard-code appropriate expected ranges.
- At minimum, assert all frequencies are in the physically reasonable range (400-4500 cm-1 for water)

**Test 3: test_ch4_reference_output_structure(ch4_results_json)**
- Load JSON
- Assert expected top-level keys present
- Assert harmonic frequency count matches CH4 expectations (9 modes or 4 unique frequencies if degenerate modes are collapsed -- check the actual JSON)

**Test 4: test_ch4_reference_output_values(ch4_results_json)**
- Verify CH4 frequencies are in physically reasonable range
- CH4 has fundamentals around ~1300 and ~3000 cm-1
- Assert at least one frequency in 1200-1400 range and at least one in 2800-3200 range

**Test 5: test_reference_outputs_are_committed**
- Use `fixtures_dir` fixture
- Assert `(fixtures_dir / "water" / "results.json").exists()`
- Assert `(fixtures_dir / "CH4_ase" / "results.json").exists()`
- This is a meta-test that verifies the fixture files are actually present

**Test 6: test_marker_filtering_unit_only**
- This is NOT an actual pytest test -- instead, document in a comment that marker filtering is verified by running: `pytest -m "not gpu and not gaussian and not slow" --co -q` and confirming all collected tests are unit tests.
- Instead, create a simple test that just passes, marked with no special markers, to serve as a "unit test" baseline:
  ```python
  def test_unit_test_baseline():
      """This test should always be collected when running with -m 'not gpu and not gaussian'."""
      assert True
  ```

**Test 7: test_gpu_marker_exists**
- Create a test marked `@pytest.mark.gpu` that is trivially true
- This test should be SKIPPED when running `pytest -m "not gpu"` and COLLECTED when running `pytest -m gpu`
  ```python
  @pytest.mark.gpu
  def test_gpu_placeholder():
      """Placeholder GPU test -- verifies marker infrastructure works."""
      pytest.skip("No GPU test implemented yet -- marker infrastructure verification only")
  ```

**Test 8: test_gaussian_marker_exists**
- Same pattern with `@pytest.mark.gaussian`
  ```python
  @pytest.mark.gaussian
  def test_gaussian_placeholder():
      """Placeholder Gaussian test -- verifies marker infrastructure works."""
      pytest.skip("No Gaussian test implemented yet -- marker infrastructure verification only")
  ```

**IMPORTANT:** Before writing the tests, read the actual `tests/fixtures/water/results.json` and `tests/fixtures/CH4_ase/results.json` to understand the JSON structure. The key names and nesting will determine the assertions. Do not assume a structure -- use the actual data.
  </action>
  <verify>
Run: `python -m pytest tests/test_regression.py -v` -- all tests pass (marker placeholders skip as expected).
Run: `python -m pytest tests/test_regression.py -v -m "not gpu and not gaussian"` -- marker placeholder tests are excluded.
Run: `python -m pytest tests/ --cov --cov-report=term-missing -q 2>&1 | tail -20` -- coverage report shows module coverage percentages.
Run: `python -m pytest tests/ --cov --cov-report=term-missing -q 2>&1 | grep "gaussian_parser"` -- gaussian_parser module appears in coverage output.
  </verify>
  <done>tests/test_regression.py contains 8 tests: water reference structure and values, CH4 reference structure and values, fixture existence check, unit test baseline, GPU marker placeholder, Gaussian marker placeholder. Coverage report runs successfully and shows coverage for core modules. Requirements TEST-05, TEST-06, TEST-07, TEST-09 satisfied.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v` shows all tests across all test files
- `python -m pytest tests/ --cov --cov-report=term-missing -q` shows coverage for gaussian_parser, fchk_parser, mode_matching
- `python -m pytest tests/ -m "not gpu and not gaussian" -v` collects only unit tests
- `python -m pytest tests/ -m "gpu" --co -q` collects only GPU-marked tests
- Total test count across all files is >= 25
</verification>

<success_criteria>
- Water results.json validated: structure correct, frequency values in reasonable range
- CH4 results.json validated: structure correct, frequency values in reasonable range
- Marker filtering works: `pytest -m "not gpu and not gaussian"` excludes integration tests
- Coverage report runs: `pytest --cov` shows percentages for gaussian_parser, fchk_parser, mode_matching
- All committed reference outputs are present and loadable
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-infrastructure-characterization/01-04-SUMMARY.md`
</output>
