---
phase: 09-ci-cd-distribution-prep
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - CI-04

must_haves:
  truths:
    - "README Installation section shows all 5 explicit steps (clone, conda env, pip install mace_ML_pkg, pip install mace_dipole_pkg, pip install main pkg)"
    - "README references environment.yml by name"
    - "README shows python cli.py diagnose as the verification command"
    - "Each pip install step is a separate explicit command (not just 'run the script')"
  artifacts:
    - path: "README.md"
      provides: "Updated Installation section with manual step-by-step"
      contains: "pip install -e mace_ML_pkg"
  key_links:
    - from: "README.md Installation section"
      to: "environment.yml"
      via: "micromamba env create -f environment.yml reference"
      pattern: "environment.yml"
---

<objective>
Rewrite the README Installation section to show explicit step-by-step instructions that a
general researcher can follow on their own machine to reproduce the environment.

Purpose: CI-04 requires a documented install procedure. The current README says "run
install_mace_packages.sh" without showing what it does. A researcher on a new machine
needs to see each pip install command explicitly.
Output: README.md with a rewritten Installation section covering all 5 required steps.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ci-cd-distribution-prep/09-CONTEXT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README Installation section with explicit manual steps</name>
  <files>README.md</files>
  <action>
Replace the current "## Installation" section (lines 14-61) with the rewritten version below.
Keep everything before line 14 (Features) and after line 62 (Usage) unchanged.

The new Installation section must cover these 5 steps explicitly (per locked decision CI-04):
1. Clone the repo
2. Create a conda/micromamba environment referencing environment.yml
3. Install mace_ML_pkg with pip install -e
4. Install mace_dipole_pkg with pip install -e
5. Install the main package with pip install -e .
6. Verify with python cli.py diagnose

The install_mace_packages.sh script can be mentioned as a convenience shortcut AFTER
showing the manual steps (per research note on Pitfall 5: keep the script, but show manual
steps first).

New Installation section content:

---

## Installation

### Prerequisites

- **Python 3.10** (required — matches the `mace4ir_v2` environment)
- **Gaussian 16** (must be in PATH as `g16`)
- **CUDA-capable GPU** (required for ML calculations; CPU fallback is slow)
- **micromamba or conda** (for environment management)

### Step-by-Step Installation

**1. Clone the repository**

```bash
git clone https://github.com/your-username/mace_gaussian.git
cd mace_gaussian
```

**2. Create the conda environment**

The repository includes `environment.yml` with the exact package versions used for development.

```bash
# Install micromamba if needed
"${SHELL}" <(curl -L micro.mamba.pm/install.sh)

# Create and activate the environment
micromamba env create -f environment.yml
micromamba activate mace4ir_v2
```

**3. Install the custom MACE packages**

This project depends on two local MACE forks included in the repository.
They must be installed in editable mode so their paths resolve correctly:

```bash
# Install standard MACE (energy/force calculator)
pip install -e mace_ML_pkg

# Install MACE with dipole support (dipole derivative calculator)
pip install -e mace_dipole_pkg
```

These packages are not on PyPI and must be installed from the local subdirectories.

**4. Install the main package**

```bash
pip install -e .
```

This installs the `mace_gaussian` package in editable mode so CLI commands and imports work.

**5. Verify the installation**

```bash
python cli.py diagnose
```

This checks for Gaussian 16 (`g16`), `formchk`, CUDA availability, and all dipole calculators.
A successful install shows green checkmarks for the calculators; Gaussian/CUDA checks may
show warnings if those are not available on the current machine.

### Environment Files

The repository includes:
- `environment.yml` — Complete conda/micromamba environment with exact package versions
- `requirements_mace4ir_v2.txt` — Pip package list for reference
- `install_mace_packages.sh` — Convenience script that runs steps 3 above automatically

---

Replace only the Installation section. All other README sections (Features, Usage, Workflow
Overview, Output Structure, Configuration, Troubleshooting, Dependencies, For Developers,
Common Issues) remain unchanged.
  </action>
  <verify>
    <automated>
      python3 -c "
content = open('/home/mot/mace_gaussian/README.md').read()
assert 'pip install -e mace_ML_pkg' in content, 'Missing mace_ML_pkg install step'
assert 'pip install -e mace_dipole_pkg' in content, 'Missing mace_dipole_pkg install step'
assert 'environment.yml' in content, 'Missing environment.yml reference'
assert 'python cli.py diagnose' in content, 'Missing diagnose step'
assert 'pip install -e .' in content, 'Missing main package install step'
print('README install section contains all required steps')
"
    </automated>
    <manual>Read the Installation section and confirm a researcher could follow it cold on a new machine without guessing any commands. Each step should be explicit and numbered.</manual>
  </verify>
  <done>
    README.md Installation section shows all 5 explicit steps.
    Each pip install command is shown separately (not hidden behind a script).
    environment.yml is referenced by name.
    python cli.py diagnose is the final verification command.
    install_mace_packages.sh is mentioned as a convenience alternative after the manual steps.
  </done>
</task>

</tasks>

<verification>
Automated check:
  python3 -c "
  content = open('README.md').read()
  checks = [
      ('pip install -e mace_ML_pkg', 'mace_ML_pkg install'),
      ('pip install -e mace_dipole_pkg', 'mace_dipole_pkg install'),
      ('environment.yml', 'environment.yml reference'),
      ('python cli.py diagnose', 'diagnose command'),
      ('pip install -e .', 'main package install'),
  ]
  for text, label in checks:
      status = 'OK' if text in content else 'MISSING'
      print(f'{status}: {label}')
  "

All 5 checks should print OK.
</verification>

<success_criteria>
- README.md Installation section shows all 5 explicit steps in numbered order
- Each pip install is a separate visible command (not hidden in a script reference)
- environment.yml referenced by name so researcher knows what file to use
- python cli.py diagnose is the final verification command
- Section reads naturally — a researcher unfamiliar with the project can follow it cold
</success_criteria>

<output>
After completion, create `.planning/phases/09-ci-cd-distribution-prep/09-03-SUMMARY.md`
</output>
