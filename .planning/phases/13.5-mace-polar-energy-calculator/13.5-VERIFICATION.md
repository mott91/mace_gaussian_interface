---
phase: 13.5-mace-polar-energy-calculator
verified: 2026-03-23T09:15:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 13.5: MACE-POLAR-1 Energy Calculator Verification Report

**Phase Goal:** Integrate MACE-POLAR-1 (`polar-1-m`) as a new energy calculator option. Wire into `--energy-calculators` and `--optimization-calculator` CLI options.
**Verified:** 2026-03-23T09:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                         | Status     | Evidence                                                                                             |
|----|---------------------------------------------------------------------------------------------------------------|------------|------------------------------------------------------------------------------------------------------|
| 1  | `from mace.calculators import mace_polar` succeeds (mace-torch upgraded to main branch)                       | VERIFIED   | `python -c "from mace.calculators import mace_polar; print('OK')"` exits 0; mace-torch version 0.3.15 |
| 2  | Existing calculator tests (mace_mp, mace_off, mace_omol, mace_anicc) all pass after mace-torch upgrade        | VERIFIED   | 20/20 tests in `test_workflow_calculator.py` pass including all prior-existing test classes          |
| 3  | User can pass `mace_polar` to `--energy-calculators` and CLI accepts it                                       | VERIFIED   | `VALID_ENERGY_CALCULATORS` includes `"mace_polar"` at cli.py:37; validation callback accepts it      |
| 4  | User can pass `mace_polar` to `--optimization-calculator` and CLI accepts it                                   | VERIFIED   | `click.Choice` at cli.py:77 includes `"mace_polar"`; help output confirms the choice                 |
| 5  | `calculator("mace_polar")` returns an ASE calculator via `mace_polar(model="polar-1-l", device="cuda", default_dtype="float64")` | VERIFIED   | workflow.py:316-320 implements the branch; 4 TestCalculatorMacePolar tests pass                      |
| 6  | DFT baseline config exists for `mace_polar` (B3LYP/6-31G(d,p))                                               | VERIFIED   | `DFT_BASELINES["mace_polar"]` at dft_baseline.py:71-76; method=b3lyp, basis=6-31G(d,p)              |
| 7  | No `dispersion` kwarg is passed to `mace_polar` (unlike mace_mp/mace_off/mace_omol)                           | VERIFIED   | workflow.py:319 branch: `mace_polar(model="polar-1-l", device="cuda", default_dtype="float64")` — no `dispersion`; confirmed by `test_mace_polar_does_not_pass_dispersion` |
| 8  | Default `--energy-calculators` does NOT include `mace_polar` (opt-in only)                                    | VERIFIED   | cli.py:82 default is `"mace_mp,mace_omol,mace_anicc,mace_off"`; CLI help confirmed no `mace_polar` before `energy-calculators` section |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact                              | Expected                                          | Status     | Details                                                                 |
|---------------------------------------|---------------------------------------------------|------------|-------------------------------------------------------------------------|
| `mace_gaussian/workflow.py`           | mace_polar branch in `calculator()` if-chain      | VERIFIED   | Lines 316-320: `if nnp == "mace_polar":` branch with lazy import        |
| `mace_gaussian/cli.py`                | mace_polar in VALID_ENERGY_CALCULATORS and Choice | VERIFIED   | Line 37: constant includes `"mace_polar"`; line 77: Click.Choice includes it |
| `mace_gaussian/dft_baseline.py`       | mace_polar entry in DFT_BASELINES dict            | VERIFIED   | Lines 71-76: full entry with method/basis/description/extra_keywords    |
| `tests/test_workflow_calculator.py`   | TestCalculatorMacePolar class (4 tests)           | VERIFIED   | Lines 389-455: class exists with 4 passing tests + `_make_import_interceptor_polar` helper |
| `tests/test_cli_validation.py`        | 4 mace_polar CLI tests                            | VERIFIED   | Lines 66-122: `test_mace_polar_accepted`, `test_valid_energy_calculators_includes_mace_polar`, `test_default_energy_calculators_does_not_include_mace_polar`, `test_mace_polar_accepted_by_click` |

All 5 artifacts: exist, are substantive (not stubs), and wired into their respective modules/test runners.

---

### Key Link Verification

| From                  | To                            | Via                                                     | Status   | Details                                                        |
|-----------------------|-------------------------------|---------------------------------------------------------|----------|----------------------------------------------------------------|
| `mace_gaussian/cli.py` | `mace_gaussian/workflow.py`  | VALID_ENERGY_CALCULATORS includes mace_polar → calculator("mace_polar") branch exists | WIRED | cli.py:37 constant + workflow.py:316 branch both contain `mace_polar` |
| `mace_gaussian/workflow.py` | `mace.calculators.mace_polar` | lazy import in calculator() branch                    | WIRED    | workflow.py:317: `from mace.calculators import mace_polar`; import verified live in mace-torch 0.3.15 |

---

### Requirements Coverage

| Requirement | Source Plan  | Description                                                                                           | Status    | Evidence                                                                       |
|-------------|--------------|-------------------------------------------------------------------------------------------------------|-----------|--------------------------------------------------------------------------------|
| POLAR-01    | 13.5-01-PLAN | User can specify `mace_polar` as `--energy-calculators` choice in CLI                                 | SATISFIED | `"mace_polar"` in `VALID_ENERGY_CALCULATORS`; `_validate_energy_calculators` callback accepts it; `test_mace_polar_accepted` passes |
| POLAR-02    | 13.5-01-PLAN | User can specify `mace_polar` as `--optimization-calculator` choice in CLI                            | SATISFIED | `"mace_polar"` in `click.Choice` at cli.py:77; `test_mace_polar_accepted_by_click` passes |
| POLAR-03    | 13.5-01-PLAN | `calculator("mace_polar")` calls `mace_polar(model="polar-1-l", device="cuda", default_dtype="float64")` with no dispersion kwarg, DFT baseline config exists | SATISFIED | workflow.py:316-320 branch confirmed; DFT_BASELINES entry confirmed; `TestCalculatorMacePolar` (4 tests) all pass |

No orphaned requirements found — REQUIREMENTS.md maps POLAR-01/02/03 to Phase 13.5 and all three are claimed by 13.5-01-PLAN.

---

### Anti-Patterns Found

No anti-patterns found in the 5 modified files. No TODO/FIXME/PLACEHOLDER markers. No stub implementations (empty return or console-log-only handlers). No return null/return {} in the new branches.

---

### Human Verification Required

None — all goal-relevant behaviors are verifiable programmatically for this phase. The integration adds backend wiring only (no UI/visual behavior, no real-time behavior, no external service calls beyond model download which is already cached).

---

## Gaps Summary

No gaps. All 8 must-haves verified, all 5 artifacts confirmed substantive and wired, both key links connected, all 3 requirement IDs satisfied. Full test suite (20/20 workflow calculator tests) passes including all pre-existing regression tests for mace_mp, mace_off, mace_omol, and mace_anicc.

---

_Verified: 2026-03-23T09:15:00Z_
_Verifier: Claude (gsd-verifier)_
