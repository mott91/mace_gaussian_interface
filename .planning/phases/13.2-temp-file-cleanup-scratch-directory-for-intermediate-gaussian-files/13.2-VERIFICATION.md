---
phase: 13.2-temp-file-cleanup-scratch-directory-for-intermediate-gaussian-files
verified: 2026-03-14T20:10:00Z
status: passed
score: 15/15 must-haves verified
re_verification: false
---

# Phase 13.2: Scratch Directory Isolation — Verification Report

**Phase Goal:** All intermediate Gaussian files (.gjf, .log, .chk, IPC socket) are created inside
per-run `.scratch/` subdirectories instead of the project root; scratch dirs are auto-cleaned on
success, failure, and startup (stale >24h); `--keep-scratch` CLI flag preserves scratch on failure
for debugging.
**Verified:** 2026-03-14T20:10:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from Plan 01 and Plan 02 must_haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `scratch_dir()` context manager creates per-run dir under `.scratch/` and deletes it on normal exit | VERIFIED | `scratch.py` lines 44-62; test `test_deletes_directory_on_normal_exit` passes |
| 2 | `scratch_dir()` preserves scratch dir on failure when `keep_on_failure=True` | VERIFIED | `scratch.py` lines 51-54; test `test_keeps_directory_on_failure_when_flag_set` passes |
| 3 | Stale scratch dirs older than 24h are detected and cleaned up | VERIFIED | `cleanup_stale_scratch_dirs` + `list_stale_scratch_dirs` in `scratch.py`; tests pass |
| 4 | `runner.py` accepts `cwd` and `env` parameters for subprocess isolation | VERIFIED | `runner.py` lines 31-32, 76-77: both params passed to `subprocess.Popen` |
| 5 | `gm_helper.py` reads `MACE_IPC_PATH` env var with `.ipc_file` fallback | VERIFIED | `gm_helper.py` line 29: `os.environ.get("MACE_IPC_PATH", ".ipc_file")` |
| 6 | `ase_to_gjf` and `create_gaussian_dft_input` accept `output_dir` parameter | VERIFIED | `io.py` lines 150, 159-166; `dft_baseline.py` lines 140, 176-183 |
| 7 | ML frequency calculations write all intermediate files into `.scratch/` subdirectory | VERIFIED | `workflow.py` lines 458-492: `scratch_dir` context manager wraps all Gaussian I/O; `output_dir=scratch_path`, `cwd=str(scratch_path)` |
| 8 | DFT baseline calculations write all intermediate files into `.scratch/` subdirectory | VERIFIED | `dft_baseline.py` lines 335-400: same pattern for DFT path |
| 9 | On success, Gaussian output files are moved from scratch to final results directory | VERIFIED | `workflow.py` lines 485-492 (ML); `dft_baseline.py` lines 393-400 (DFT): `shutil.move` from `scratch_path` to `freq_dir` before context exits |
| 10 | On failure, scratch directory is auto-deleted (unless `--keep-scratch`) | VERIFIED | `scratch_dir` `except BaseException` branch calls `shutil.rmtree` unless `keep_on_failure=True` |
| 11 | `mace-gaussian run --keep-scratch` preserves scratch dir on failure | VERIFIED | `cli.py` lines 105-108 (flag), 133-134 (env var fallback), 203 (passed to `run_pipeline`) |
| 12 | `MACE_KEEP_SCRATCH=1` env var preserves scratch dir on failure | VERIFIED | `cli.py` lines 133-134: `os.environ.get("MACE_KEEP_SCRATCH", "0") == "1"` |
| 13 | Stale scratch dirs older than 24h are auto-cleaned on startup | VERIFIED | `cli.py` lines 129, 185-187: `cleanup_stale_scratch_dirs()` called before `run_pipeline` |
| 14 | `mace-gaussian diagnose` reports stale scratch dirs | VERIFIED | `cli.py` lines 418, 429-437: `list_stale_scratch_dirs()` called in `diagnose` with output |
| 15 | IPC socket lives inside scratch dir with absolute path via `MACE_IPC_PATH` env var | VERIFIED | `workflow.py` lines 468-472: `ipc_path = str((scratch_path / "zmq.ipc").resolve())`, set in `env["MACE_IPC_PATH"]` passed to Popen (not global `os.environ`) |

**Score:** 15/15 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mace_gaussian/utils/scratch.py` | `scratch_dir`, `cleanup_stale_scratch_dirs`, `list_stale_scratch_dirs`, `SCRATCH_ROOT` | VERIFIED | 129 lines; all three exports + constants present; no placeholders |
| `tests/test_scratch.py` | Unit tests for scratch utility (min 50 lines) | VERIFIED | 131 lines; 8 tests across 3 classes; all 8 pass |
| `mace_gaussian/gaussian/runner.py` | `cwd` and `env` params on `run_gaussian_with_zmq` | VERIFIED | Lines 31-32 declare params; lines 76-77 pass to Popen |
| `mace_gaussian/gm_helper.py` | `MACE_IPC_PATH` env var read | VERIFIED | Line 29: `os.environ.get("MACE_IPC_PATH", ".ipc_file")` |
| `mace_gaussian/gaussian/io.py` | `output_dir` param on `ase_to_gjf` | VERIFIED | Line 150; logic at lines 159-169 |
| `mace_gaussian/dft_baseline.py` | `output_dir` on `create_gaussian_dft_input`; `cwd` on `run_gaussian_dft`; `scratch_dir` in `run_dft_baseline_calculation`; `keep_scratch` threaded through `run_all_dft_baselines` | VERIFIED | All four changes confirmed |
| `mace_gaussian/workflow.py` | `scratch_dir` context manager in `run_frequency_calculation`; `keep_scratch` threaded from `run_pipeline` | VERIFIED | Lines 40, 458, 600, 640 |
| `mace_gaussian/cli.py` | `--keep-scratch` flag; stale cleanup on startup; diagnose stale reporting | VERIFIED | Lines 105-108, 185-187, 429-437 |
| `.gitignore` | `.scratch/` entry | VERIFIED | Line 70 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `workflow.py` | `mace_gaussian/utils/scratch.py` | `from .utils.scratch import scratch_dir` | WIRED | Line 40 import; `scratch_dir(...)` called at line 458 |
| `workflow.py` | `run_gaussian_with_zmq` | `cwd=str(scratch_path), env=env` | WIRED | Lines 480-481; both params pass scratch path and env |
| `workflow.py` | `MACE_IPC_PATH` | `env["MACE_IPC_PATH"] = ipc_path` in env dict passed to Popen | WIRED | Lines 472, 481: uses `env=` parameter, not global `os.environ` mutation |
| `dft_baseline.py` | `mace_gaussian/utils/scratch.py` | `from .utils.scratch import scratch_dir` | WIRED | Line 20 import; `scratch_dir(...)` called at line 335 |
| `cli.py` | `mace_gaussian/utils/scratch.py` | `cleanup_stale_scratch_dirs` on startup; `list_stale_scratch_dirs` in diagnose | WIRED | Lines 129, 185-187 (run); lines 418, 429-437 (diagnose) |
| `cli.py` | `workflow.py` | `keep_scratch` param threaded to `run_pipeline` | WIRED | Line 203: `run_pipeline(..., keep_scratch=keep_scratch)` |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SCRATCH-01 | 13.2-01, 13.2-02 | All intermediate Gaussian files (.gjf, .log, .chk, .ipc_file) created inside `.scratch/` subdirectory, not project root — both ML frequency and DFT baseline runs | SATISFIED | `workflow.py` lines 458-492; `dft_baseline.py` lines 335-400; `output_dir=scratch_path`, `cwd=str(scratch_path)` on all Gaussian invocations |
| SCRATCH-02 | 13.2-01, 13.2-02 | Scratch directory auto-deleted on success and on failure (unless `--keep-scratch` or `MACE_KEEP_SCRATCH=1`) | SATISFIED | `scratch_dir` context manager (success: else branch; failure: except BaseException); CLI flag + env var resolved in `cli.py` lines 133-134; threaded to `scratch_dir(keep_on_failure=keep_scratch)` |
| SCRATCH-03 | 13.2-02 | `mace-gaussian run` auto-cleans stale scratch dirs (>24h) on startup; `mace-gaussian diagnose` reports stale scratch dirs | SATISFIED | `cli.py` `run` command: `cleanup_stale_scratch_dirs()` at line 185; `diagnose` command: `list_stale_scratch_dirs()` at line 429 |

No orphaned requirements: all three SCRATCH-0x IDs are covered by the plans and satisfied by implementation.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `mace_gaussian/workflow.py` | 165 | Line too long (109 > 100) — `logger.debug` for dipole timing | Info | Pre-existing before Phase 13.2 (present in commit `ae0090a`); not introduced by this phase |

No blocker anti-patterns introduced by Phase 13.2.

---

### Human Verification Required

**None identified.** All truths are verifiable by static code analysis and unit tests. The actual end-to-end behavior (Gaussian subprocess runs inside `.scratch/`, scratch dirs physically absent from project root after a real run) requires Gaussian 16 to be available, but the code path is fully wired and the unit tests confirm the context manager behavior.

---

### Gaps Summary

No gaps. All 15 must-have truths are verified. All three requirements (SCRATCH-01, SCRATCH-02, SCRATCH-03) are fully satisfied. The implementation matches the plan precisely:

- `scratch.py` provides the context manager and cleanup utilities with correct BaseException handling.
- All Gaussian invocations (ML via `workflow.py`, DFT via `dft_baseline.py`) are wrapped in `scratch_dir`, write files to `scratch_path`, and move outputs to `freq_dir` before the context exits.
- IPC socket uses an absolute path inside the scratch dir, injected via `env=` dict to avoid polluting the global environment.
- `keep_scratch` threads from CLI flag / `MACE_KEEP_SCRATCH` env var through `run_pipeline` to every `scratch_dir(keep_on_failure=...)` call.
- Startup stale cleanup and diagnose reporting are wired in `cli.py`.
- 8 unit tests cover all critical paths and all pass.
- `.gitignore` excludes `.scratch/`.

---

_Verified: 2026-03-14T20:10:00Z_
_Verifier: Claude (gsd-verifier)_
