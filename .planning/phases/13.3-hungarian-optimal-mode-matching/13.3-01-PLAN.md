---
phase: 13.3-hungarian-optimal-mode-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mace_gaussian/analysis/mode_matching.py
  - tests/test_mode_matching.py
autonomous: true
requirements:
  - MATCH-01
  - MATCH-02

must_haves:
  truths:
    - "match_modes() returns globally optimal bijective 1-to-1 mode pairing"
    - "Two ML modes cannot claim the same DFT mode"
    - "Unmatched modes (when n_calc != n_ref) appear as (None, 0.0) in results"
    - "Low-overlap pairs are logged as warnings but kept in the dict"
  artifacts:
    - path: "mace_gaussian/analysis/mode_matching.py"
      provides: "Hungarian-based match_modes() using linear_sum_assignment"
      contains: "linear_sum_assignment"
    - path: "tests/test_mode_matching.py"
      provides: "Bijection test proving greedy failure scenario is fixed"
      contains: "test_hungarian_prevents_duplicate_assignment"
  key_links:
    - from: "mace_gaussian/analysis/mode_matching.py"
      to: "scipy.optimize.linear_sum_assignment"
      via: "import and call with negated overlap matrix"
      pattern: "linear_sum_assignment\\(-overlap"
    - from: "mace_gaussian/analysis/mode_matching.py"
      to: "create_alignment_matrix"
      via: "reuse existing overlap matrix builder"
      pattern: "create_alignment_matrix\\(modes_calc, modes_ref\\)"
---

<objective>
Replace the greedy mode matching algorithm in `match_modes()` with the Hungarian algorithm
(`scipy.optimize.linear_sum_assignment`) for globally optimal 1-to-1 ML-to-DFT mode pairing.
Add the bijection correctness test and handle unequal mode counts.

Purpose: Fixes correctness bug where greedy matching allows two ML modes to claim the same
DFT mode, producing incorrect regression metrics before the benchmark campaign.

Output: Updated `match_modes()` function + comprehensive tests including bijection proof.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.3-hungarian-optimal-mode-matching/13.3-CONTEXT.md
@.planning/phases/13.3-hungarian-optimal-mode-matching/13.3-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From mace_gaussian/analysis/mode_matching.py (current signature to replace):
```python
def match_modes(
    modes_calc: np.ndarray, modes_ref: np.ndarray, threshold: float = 0.5
) -> dict[int, tuple[int, float]]:
    """Maps calc_mode_idx -> (ref_mode_idx, overlap)"""
```

From mace_gaussian/analysis/mode_matching.py (reuse this — do NOT duplicate):
```python
def create_alignment_matrix(modes_calc: np.ndarray, modes_ref: np.ndarray) -> np.ndarray:
    """Returns shape (n_modes_calc, n_modes_ref), element [i,j] is overlap."""
```

Call sites that consume match_modes() return value:
- analysis_workflow.py:364 — `mode_mapping = {ml_idx: dft_idx for ml_idx, (dft_idx, overlap) in matches.items()}`
- analysis_workflow.py:365 — `mode_overlaps = {ml_idx: overlap for ml_idx, (dft_idx, overlap) in matches.items()}`
- analysis_workflow.py:612 — `match_modes(modes_ml, modes_dft)` (return value currently unused for heatmap)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace greedy match_modes() with Hungarian algorithm + tests</name>
  <files>mace_gaussian/analysis/mode_matching.py, tests/test_mode_matching.py</files>
  <behavior>
    - Test: identical modes still produce 1-to-1 identity mapping with overlap ~1.0
    - Test: permuted modes correctly recover original mapping (existing test unchanged)
    - Test: two calc modes both closest to same ref mode under greedy are correctly separated by Hungarian (bijection test)
    - Test: rectangular matrix (3 calc, 5 ref) assigns 3 pairs, unmatched calc modes do NOT appear
    - Test: rectangular matrix (5 calc, 3 ref) assigns 3 pairs, 2 unmatched calc modes get (None, 0.0)
    - Test: low-overlap pairs are kept in dict (not dropped), overlap value preserved
  </behavior>
  <action>
    1. In `mode_matching.py`, replace the greedy loop in `match_modes()` (lines 161-214) with:
       - Add `from scipy.optimize import linear_sum_assignment` at top of file
       - Call `create_alignment_matrix(modes_calc, modes_ref)` to build the full overlap matrix (reuse existing function per user decision)
       - Call `linear_sum_assignment(-overlap_matrix)` to get globally optimal assignment (negate for maximization)
       - Build matches dict from row_ind/col_ind pairs
       - For any calc index not in row_ind (when n_calc > n_ref), set `matches[i] = (None, 0.0)`
       - Log `logger.warning` for pairs with overlap < threshold (default 0.5) — but keep them in dict per user decision
       - Log `logger.debug` for confident matches
       - Update return type annotation to `dict[int, tuple[int | None, float]]`

    2. In `tests/test_mode_matching.py`, add new test class `TestHungarianMatching`:
       - `test_hungarian_prevents_duplicate_assignment`: 3 atoms, 3 modes scenario from RESEARCH.md where calc modes 0 and 1 are both closest to ref mode 0 under greedy. Assert all assigned refs are unique (bijective). Assert calc mode 2 -> ref mode 2.
       - `test_unmatched_modes_more_calc_than_ref`: 5 calc modes, 3 ref modes. Assert 3 matched pairs + 2 (None, 0.0) entries. Assert len(matches) == 5.
       - `test_unmatched_modes_more_ref_than_calc`: 3 calc modes, 5 ref modes. Assert all 3 have valid matches (no None). Assert len(matches) == 3.
       - `test_low_overlap_pairs_kept`: Create nearly orthogonal modes, verify low-overlap pair is in dict with overlap value preserved (not dropped).

    3. Ensure existing tests pass unchanged — they use equal-sized mode arrays so None entries won't appear.

    IMPORTANT: Do NOT change the function signature (name, parameters, default threshold). Only the return type annotation changes from `tuple[int, float]` to `tuple[int | None, float]`.
    IMPORTANT: Do NOT pad rectangular matrices to make them square — scipy handles rectangular natively.
    IMPORTANT: Do NOT filter negative frequencies — they are valid data points per project convention.
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian && python -m pytest tests/test_mode_matching.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - match_modes() uses linear_sum_assignment for globally optimal bijective assignment
    - Existing tests pass unchanged
    - Bijection test proves greedy failure scenario is fixed by Hungarian
    - Rectangular matrix tests prove unmatched modes get (None, 0.0)
    - All tests green
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard downstream call sites for None ref_idx</name>
  <files>mace_gaussian/analysis/analysis_workflow.py</files>
  <action>
    At analysis_workflow.py:364, the dict comprehension unpacks `(dft_idx, overlap)` from matches.
    With the new return type, `dft_idx` can be `None` for unmatched modes.

    Update the two dict comprehensions at lines 364-365:
    ```python
    mode_mapping = {ml_idx: dft_idx for ml_idx, (dft_idx, overlap) in matches.items() if dft_idx is not None}
    mode_overlaps = {ml_idx: overlap for ml_idx, (dft_idx, overlap) in matches.items() if dft_idx is not None}
    ```

    This is a minimal guard — unmatched modes (None) are simply excluded from the mapping/overlaps dicts.
    The downstream code (match_by_mode, plot_regression, etc.) already handles missing entries gracefully
    since mode_mapping is keyed by ml_idx and only matched modes participate.

    Also at line 612, store the match_modes result and pass it to the heatmap call (currently unused):
    ```python
    matches = match_modes(modes_ml, modes_dft)
    ```
    This line already exists and captures the result. The heatmap wiring happens in Plan 02.
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian && python -c "from mace_gaussian.analysis.analysis_workflow import HarmonicAnalysisWorkflow; print('Import OK')"</automated>
  </verify>
  <done>
    - analysis_workflow.py call sites at lines 364-365 filter out None entries from match_modes return
    - No TypeError when n_calc != n_ref
  </done>
</task>

</tasks>

<verification>
All mode matching tests pass: `python -m pytest tests/test_mode_matching.py -x -v`
Import check: `python -c "from mace_gaussian.analysis.mode_matching import match_modes; print('OK')"`
Import check: `python -c "from mace_gaussian.analysis.analysis_workflow import HarmonicAnalysisWorkflow; print('OK')"`
</verification>

<success_criteria>
- match_modes() returns globally optimal bijective assignment via Hungarian algorithm
- Unmatched modes appear as (None, 0.0) when mode counts differ
- Low-overlap pairs logged but kept in dict
- Existing tests pass unchanged; new bijection test proves correctness improvement
- Downstream call sites handle None ref_idx without error
</success_criteria>

<output>
After completion, create `.planning/phases/13.3-hungarian-optimal-mode-matching/13.3-01-SUMMARY.md`
</output>
