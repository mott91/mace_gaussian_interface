---
phase: 04-extract-calculator-classes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calculators/__init__.py
  - calculators/base.py
  - calculators/espaloma.py
  - calculators/mace_ml.py
  - calculators/xtb.py
  - calculators/factory.py
  - gm_main.py
autonomous: true
requirements: [STRUCT-02]

must_haves:
  truths:
    - "DipoleCalculatorBase ABC exists in calculators/base.py with calculate_dipole and calculate_dipole_derivatives methods"
    - "All 3 calculator implementations exist as separate modules in calculators/ package"
    - "DipoleCalculatorFactory and dipole_factory singleton are importable from calculators package"
    - "gm_main.py imports dipole_factory from calculators instead of defining it inline"
    - "Existing tests still pass after extraction"
  artifacts:
    - path: "calculators/__init__.py"
      provides: "Package re-exports for base, factory, implementations, and dipole_factory singleton"
      contains: "__all__"
    - path: "calculators/base.py"
      provides: "DipoleCalculatorBase ABC"
      exports: ["DipoleCalculatorBase"]
    - path: "calculators/espaloma.py"
      provides: "EspalomaDipoleCalculator"
      exports: ["EspalomaDipoleCalculator"]
    - path: "calculators/mace_ml.py"
      provides: "MACEMLDipoleCalculator with DEFAULT_MACE_DIPOLE_MODEL constant"
      exports: ["MACEMLDipoleCalculator"]
    - path: "calculators/xtb.py"
      provides: "XTBDipoleCalculator"
      exports: ["XTBDipoleCalculator"]
    - path: "calculators/factory.py"
      provides: "DipoleCalculatorFactory class and dipole_factory singleton"
      exports: ["DipoleCalculatorFactory", "dipole_factory"]
  key_links:
    - from: "calculators/factory.py"
      to: "calculators/espaloma.py, calculators/mace_ml.py, calculators/xtb.py"
      via: "import and instantiation in _register_calculators"
      pattern: "from calculators\\.(espaloma|mace_ml|xtb) import"
    - from: "calculators/mace_ml.py"
      to: "mace_calculators.py"
      via: "lazy import of MACEDipoleCalculator"
      pattern: "from mace_calculators import MACEDipoleCalculator"
    - from: "gm_main.py"
      to: "calculators/__init__.py"
      via: "import dipole_factory"
      pattern: "from calculators import dipole_factory"
---

<objective>
Extract the dipole calculator class hierarchy (DipoleCalculatorBase, 3 implementations, DipoleCalculatorFactory, dipole_factory singleton) from gm_main.py into a dedicated calculators/ package, and update gm_main.py to import from the new package.

Purpose: Reduce gm_main.py by ~230 lines, improve modularity for future phases, follow the same package extraction pattern established by utils/ in Phase 3.
Output: calculators/ package with 6 files, updated gm_main.py imports
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extract-calculator-classes/04-RESEARCH.md
@.planning/phases/03-extract-utilities-conventions/03-01-SUMMARY.md
@gm_main.py (lines 86-317 — calculator hierarchy to extract)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create calculators/ package with extracted classes</name>
  <files>
    calculators/__init__.py
    calculators/base.py
    calculators/espaloma.py
    calculators/mace_ml.py
    calculators/xtb.py
    calculators/factory.py
  </files>
  <action>
Create the `calculators/` directory and 6 files by extracting code from gm_main.py lines 86-317.

**calculators/base.py**: Extract `DipoleCalculatorBase` (lines 86-139). Imports needed: `abc.ABC`, `abc.abstractmethod`, `typing.Optional`, `typing.Tuple`, `numpy`, `logging`. The class is self-contained — copy as-is with its docstrings and the `calculate_dipole_derivatives` template method.

**calculators/espaloma.py**: Extract `EspalomaDipoleCalculator` (lines 142-205). Imports needed: `from calculators.base import DipoleCalculatorBase`, `from utils.units import BOHR_TO_ANGSTROM`, `numpy`, `logging`. The heavy imports (espaloma_charge, rdkit, torch) stay as local imports inside `_check_availability()` and `calculate_dipole()` — preserve this pattern exactly.

**calculators/xtb.py**: Extract `XTBDipoleCalculator` (lines 208-238). Imports needed: `from calculators.base import DipoleCalculatorBase`, `logging`. The xtb import stays local inside `_check_availability()` and `calculate_dipole()`.

**calculators/mace_ml.py**: Extract `MACEMLDipoleCalculator` (lines 241-267). Move the `DEFAULT_MACE_DIPOLE_MODEL` constant (line 62-65 of gm_main.py) into this file since it's only used by this calculator. Imports needed: `from calculators.base import DipoleCalculatorBase`, `os`, `pathlib.Path`, `logging`. The `MACEDipoleCalculator` import from `mace_calculators` should be LAZY — import inside `_check_availability()` rather than at module level, because `mace_calculators.py` has module-level side effects (backs up sys.modules). Preserve the existing pattern where `_check_availability` instantiates `MACEDipoleCalculator(self.model_path)`.

**calculators/factory.py**: Extract `DipoleCalculatorFactory` (lines 270-313) and the `dipole_factory = DipoleCalculatorFactory()` singleton (line 317). Imports needed: `from calculators.base import DipoleCalculatorBase`, `from calculators.espaloma import EspalomaDipoleCalculator`, `from calculators.mace_ml import MACEMLDipoleCalculator`, `from calculators.xtb import XTBDipoleCalculator`, `typing.Dict`, `logging`.

**calculators/__init__.py**: Re-export pattern matching utils/__init__.py. Import and re-export: `DipoleCalculatorBase`, `EspalomaDipoleCalculator`, `XTBDipoleCalculator`, `MACEMLDipoleCalculator`, `DipoleCalculatorFactory`, `dipole_factory`. Use `__all__` list to avoid ruff F401 warnings.

Preserve exact class names — do NOT rename (EspalomaDipoleCalculator, not EspalomaCalculator). The `.name` attributes ("espaloma", "xtb", "mace_ml") are used as dict keys in the factory and as CLI arguments.
  </action>
  <verify>
Run `python -c "from calculators.base import DipoleCalculatorBase; print('base OK')"` and `python -c "from calculators import DipoleCalculatorBase, DipoleCalculatorFactory; print('package OK')"` — both should succeed without import errors. Run `ruff check calculators/ && ruff format --check calculators/` — no lint or format issues.
  </verify>
  <done>6 files exist in calculators/ package. All classes are importable individually and via the package __init__.py. No lint errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update gm_main.py to import from calculators package</name>
  <files>gm_main.py</files>
  <action>
Remove the following from gm_main.py:
1. Lines 86-317: The entire calculator hierarchy (DipoleCalculatorBase, EspalomaDipoleCalculator, XTBDipoleCalculator, MACEMLDipoleCalculator, DipoleCalculatorFactory, dipole_factory singleton) and the "MODULAR DIPOLE CALCULATION SYSTEM" section header comments
2. Line 62-65: The `DEFAULT_MACE_DIPOLE_MODEL` constant (moved to calculators/mace_ml.py)
3. Line 21: `from abc import ABC, abstractmethod` (no longer needed — verify no other ABC usage remains in gm_main.py first)
4. Line 34: `from mace_calculators import MACEDipoleCalculator` (moved to calculators/mace_ml.py)
5. Line 24: Remove `Tuple` from the typing import if it becomes unused (Dict and Optional may still be needed)

Add this import near the top of gm_main.py (with the other local imports):
```python
from calculators import dipole_factory
```

Verify that the two consumers of `dipole_factory` in gm_main.py still work:
- `run_next_calculation()` calls `dipole_factory.get_calculator(dipole_method)`
- `print_diagnostics()` calls `dipole_factory.list_available()`

These should work unchanged since the import provides the same singleton object.

Also check: if `from abc import ABC, abstractmethod` is used anywhere else in gm_main.py (e.g., other classes), keep the import. If only calculators used it, remove it. Same for `Tuple` in typing.
  </action>
  <verify>
Run `ruff check gm_main.py && ruff format --check gm_main.py` — no lint or format issues. Run `python -c "from gm_main import dipole_factory; print(type(dipole_factory))"` — should print DipoleCalculatorFactory class. Run existing tests: `python -m pytest tests/ -x -q --timeout=30` — all tests should pass.
  </verify>
  <done>gm_main.py no longer contains calculator class definitions. It imports dipole_factory from calculators package. All existing tests pass. ~230 lines removed from gm_main.py.</done>
</task>

</tasks>

<verification>
1. `python -c "from calculators import dipole_factory, DipoleCalculatorBase, DipoleCalculatorFactory"` succeeds
2. `python -c "from calculators.base import DipoleCalculatorBase"` succeeds (direct import without triggering factory)
3. `python -c "from gm_main import dipole_factory"` succeeds
4. `ruff check calculators/ gm_main.py` — no issues
5. `python -m pytest tests/ -x -q --timeout=30` — all existing tests pass
</verification>

<success_criteria>
- calculators/ package exists with 6 files (base, espaloma, mace_ml, xtb, factory, __init__)
- gm_main.py reduced by ~230 lines
- All existing tests pass without modification
- No lint errors in new or modified files
</success_criteria>

<output>
After completion, create `.planning/phases/04-extract-calculator-classes/04-01-SUMMARY.md`
</output>
