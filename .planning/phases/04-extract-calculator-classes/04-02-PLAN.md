---
phase: 04-extract-calculator-classes
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified:
  - tests/test_calculators.py
autonomous: true
requirements: [STRUCT-02]

must_haves:
  truths:
    - "Calculator interface compliance is tested: all 3 implementations are subclasses of DipoleCalculatorBase"
    - "All implementations have the required methods (calculate_dipole, calculate_dipole_derivatives, _check_availability)"
    - "Factory returns correct calculator type for each known name"
    - "Factory raises ValueError for unknown calculator name"
    - "Factory auto-selection returns first available calculator in preferred order"
    - "Factory list_available returns dict mapping names to availability booleans"
  artifacts:
    - path: "tests/test_calculators.py"
      provides: "Unit tests for calculator interface, factory pattern, and auto-selection"
      min_lines: 80
  key_links:
    - from: "tests/test_calculators.py"
      to: "calculators/base.py"
      via: "import DipoleCalculatorBase for subclass assertions"
      pattern: "from calculators\\.base import DipoleCalculatorBase"
    - from: "tests/test_calculators.py"
      to: "calculators/factory.py"
      via: "import and test DipoleCalculatorFactory"
      pattern: "from calculators\\.factory import DipoleCalculatorFactory"
---

<objective>
Write unit tests for the extracted calculators/ package covering interface compliance, factory pattern, and auto-selection logic.

Purpose: Ensure the extraction didn't break the calculator contract and the factory correctly maps names to implementations. Tests mock heavy dependencies (espaloma, xtb, MACE models) so they run without GPU or ML packages.
Output: tests/test_calculators.py with comprehensive unit tests
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extract-calculator-classes/04-RESEARCH.md
@.planning/phases/04-extract-calculator-classes/04-01-SUMMARY.md
@calculators/base.py
@calculators/factory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write calculator unit tests with mocked dependencies</name>
  <files>tests/test_calculators.py</files>
  <action>
Create `tests/test_calculators.py` following the TDD pattern. Tests must mock all heavy dependencies so they run in any environment.

**TestCalculatorInterface class** — Interface compliance tests:
- `test_is_subclass`: Parametrize over all 3 calculator classes, assert `issubclass(cls, DipoleCalculatorBase)`
- `test_has_required_methods`: Parametrize over all 3 classes, assert each has `calculate_dipole`, `calculate_dipole_derivatives`, `_check_availability`
- `test_base_class_is_abstract`: Verify `DipoleCalculatorBase` cannot be instantiated directly (raises TypeError)
- Import calculators DIRECTLY from their modules (`from calculators.base import ...`, `from calculators.espaloma import ...`) to avoid triggering factory's eager instantiation

**TestDipoleCalculatorFactory class** — Factory pattern tests:
- Patch all 3 calculator constructors to avoid real import side effects. The trick: patch `calculators.factory.EspalomaDipoleCalculator` etc. (where they're imported INTO factory.py)
- `test_get_known_calculator`: Create factory with mocked calculators (available=True), call get_calculator("espaloma") → returns correct mock
- `test_get_unknown_calculator_raises`: Call get_calculator("nonexistent") → raises ValueError with "Unknown" in message
- `test_get_unavailable_calculator_raises`: Mock calculator with available=False, call get_calculator → raises RuntimeError
- `test_auto_selects_first_available`: Mock calculators with specific availability pattern, verify auto-selection follows preferred_order
- `test_auto_raises_when_none_available`: All calculators unavailable → raises RuntimeError
- `test_list_available`: Verify returns dict of {name: bool} for all registered calculators

**TestDefaultMaceDipoleModel class** — Config constant test:
- Verify `DEFAULT_MACE_DIPOLE_MODEL` is importable from `calculators.mace_ml`
- Verify it ends with expected path suffix (`dipole_model/model_1.model`)
- Verify env var override works: patch `os.environ` with custom path, reimport and check

**Mock strategy:** Use `unittest.mock.patch` on the calculator classes at their factory import location. For individual calculator tests, patch `_check_availability` to be a no-op (set self.available = True/False) to avoid triggering real imports of espaloma/xtb/mace.

Follow existing test conventions:
- Class-based test organization (TestCalculatorInterface, TestDipoleCalculatorFactory)
- `pytest.mark.parametrize` for multi-class assertions
- Clear docstrings on test classes
  </action>
  <verify>
Run `python -m pytest tests/test_calculators.py -v --timeout=30` — all tests pass. Run `ruff check tests/test_calculators.py && ruff format --check tests/test_calculators.py` — no lint or format issues. Run full suite: `python -m pytest tests/ -x -q --timeout=30` — no regressions.
  </verify>
  <done>tests/test_calculators.py exists with tests for interface compliance (subclass checks, method presence, ABC enforcement), factory pattern (get/auto/list/error cases), and config constants. All tests pass without requiring GPU, espaloma, xtb, or MACE model files.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_calculators.py -v --timeout=30` — all tests pass
2. `ruff check tests/test_calculators.py` — no lint issues
3. `python -m pytest tests/ -x -q --timeout=30` — full suite passes (no regressions)
</verification>

<success_criteria>
- tests/test_calculators.py exists with 10+ tests covering interface, factory, and config
- All tests pass without GPU/espaloma/xtb/MACE dependencies
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-extract-calculator-classes/04-02-SUMMARY.md`
</output>
