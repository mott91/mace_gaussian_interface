---
phase: 10-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mace_gaussian/cli.py
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-04

must_haves:
  truths:
    - "python cli.py --help lists all commands with accurate, non-placeholder descriptions"
    - "python cli.py compare --help does not mention 'planned features' or list unimplemented functionality"
    - "python cli.py export --help does not mention 'planned features' or list unimplemented functionality"
    - "python cli.py diagnose --help mentions Gaussian and CUDA checks"
    - "python cli.py run --help mentions --output-dir option"
    - "README.md Installation section exists with 5-step procedure (Phase 9 verified)"
    - "README.md links to docs/quickstart.md"
  artifacts:
    - path: "mace_gaussian/cli.py"
      provides: "Accurate CLI help text for all subcommands"
    - path: "README.md"
      provides: "Installation section with link to quickstart guide"
  key_links:
    - from: "README.md"
      to: "docs/quickstart.md"
      via: "markdown hyperlink"
      pattern: "quickstart\\.md"
---

<objective>
Fix CLI placeholder docstrings and verify/extend the README so users get accurate help text.

Purpose: The `compare` and `export` commands currently describe planned features that do not exist — this misleads users. The README Installation section (written in Phase 9) needs a link to the upcoming quickstart guide. No new features are implemented here.
Output: Accurate `--help` output for all five CLI commands; README with quickstart link.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation/10-CONTEXT.md
@.planning/phases/10-documentation/10-RESEARCH.md

<interfaces>
<!-- Current CLI subcommands (from mace_gaussian/cli.py) -->
<!-- run: fully implemented — geometry opt + frequency calc pipeline -->
<!-- list: fully implemented — shows molecules and calculation details -->
<!-- diagnose: fully implemented — checks dipole calculators, mentions g16 availability in output but not in docstring -->
<!-- compare: PLACEHOLDER — prints "COMING SOON", lists "planned features" in docstring -->
<!-- export: PLACEHOLDER — prints "COMING SOON", lists "planned features" in docstring -->

<!-- Current compare docstring (lines 276-287 of cli.py): -->
"""
Compare calculation results for MOLECULE.

[PLACEHOLDER - Coming soon]

This will generate comparison tables and plots for:
- Energy comparisons
- Frequency comparisons
- Runtime analysis
- Accuracy metrics
"""

<!-- Current export docstring (lines 321-331 of cli.py): -->
"""
Export results for MOLECULE to specified format.

[PLACEHOLDER - Coming soon]

Exports all calculation results to a single file for further analysis.
"""

<!-- Target: replace misleading "planned features" with honest "not yet implemented" -->
<!-- Use run_analysis.py and run_analysis_harmonic.py as the actual analysis alternatives -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix compare and export CLI docstrings</name>
  <files>mace_gaussian/cli.py</files>
  <action>
    Edit the `compare` and `export` command docstrings in `mace_gaussian/cli.py` to remove all misleading "planned features" content. Replace with honest placeholder text.

    For `compare` (lines ~276-287), replace the docstring body with:
    ```
    Compare calculation results for MOLECULE.

    Not yet implemented. To analyze and compare results, use:

        python run_analysis.py <molecule>       (anharmonic: fundamentals, overtones, combinations)
        python run_analysis_harmonic.py <molecule>  (harmonic: fundamentals with mode matching)

    These scripts generate comparison plots, regression analysis, and an HTML report
    in the analysis_results/ directory.

    Example:
        python cli.py compare water    (not yet implemented)
    ```

    For `export` (lines ~321-331), replace the docstring body with:
    ```
    Export results for MOLECULE to specified format.

    Not yet implemented. Calculation results are stored as JSON files under
    comparison_results/<molecule>/<energy_calc>_<dipole_calc>/results.json.

    Example:
        python cli.py export water --format csv    (not yet implemented)
    ```

    Also update the `diagnose` docstring to mention what it checks more completely:
    ```
    Run diagnostic checks for available calculators and dependencies.

    Checks:
        - Available dipole calculators (espaloma, mace_ml, xtb)
        - Gaussian 16 (g16) availability in PATH
        - formchk availability in PATH
        - CUDA device availability

    Example:
        python cli.py diagnose
    ```

    Also update the `run` command docstring's Example section to include `--output-dir`:
    ```
    Example:
        python cli.py run water.xyz
        python cli.py run water.xyz --skip-dft-baseline
        python cli.py run water.xyz --energy-calculators mace_mp --dipole-calculators espaloma
        python cli.py run water.xyz --output-dir my_results
    ```

    Do NOT change any implementation code — only docstrings. Run `ruff check --fix mace_gaussian/cli.py && ruff format mace_gaussian/cli.py` after editing.
  </action>
  <verify>
    <automated>python /home/mot/mace_gaussian/cli.py compare --help 2>&1 | grep -v "planned\|Planned\|PLACEHOLDER" | head -20 && python /home/mot/mace_gaussian/cli.py export --help 2>&1 | grep -v "planned\|Planned\|PLACEHOLDER" | head -20 && python /home/mot/mace_gaussian/cli.py diagnose --help 2>&1 | grep -i "gaussian\|cuda" && python /home/mot/mace_gaussian/cli.py run --help 2>&1 | grep "output-dir"</automated>
  </verify>
  <done>
    `python cli.py compare --help` shows "Not yet implemented" with run_analysis.py alternative.
    `python cli.py export --help` shows "Not yet implemented" with JSON file location note.
    `python cli.py diagnose --help` mentions Gaussian 16 and CUDA.
    `python cli.py run --help` shows --output-dir in examples.
    No "planned features" or "PLACEHOLDER" text in any --help output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify README Installation and add quickstart link</name>
  <files>README.md</files>
  <action>
    Verify the README Installation section (written in Phase 9) contains the 5-step procedure.
    Run this check: `grep -n "pip install -e mace_ML_pkg" README.md` — must find a match.

    Then add a link to `docs/quickstart.md` in the README. Find the appropriate location
    (after the Installation section or in a "Getting Started" or "Usage" section near the top)
    and add:

    ```markdown
    ## Quickstart

    After installation, see [docs/quickstart.md](docs/quickstart.md) for a step-by-step guide
    to running your first calculation and viewing the HTML analysis report.
    ```

    If a "Usage" or "Quick Start" section already exists, update it to link to docs/quickstart.md
    rather than adding a duplicate section. Do NOT rewrite the Installation section — Phase 9
    already wrote it correctly.

    The link must use a relative path (`docs/quickstart.md`) so it works both on GitHub
    and locally. Note: docs/quickstart.md will be created in Plan 02; the link can be added
    now and will resolve once Plan 02 completes.
  </action>
  <verify>
    <automated>grep -n "pip install -e mace_ML_pkg" /home/mot/mace_gaussian/README.md && grep -n "quickstart.md" /home/mot/mace_gaussian/README.md</automated>
  </verify>
  <done>
    `grep "pip install -e mace_ML_pkg" README.md` returns a match (Installation section intact).
    `grep "quickstart.md" README.md` returns a match (link added).
    README does not duplicate the 5 installation steps in another section.
  </done>
</task>

</tasks>

<verification>
Run all CLI --help commands and verify no misleading placeholder text:

```bash
cd /home/mot/mace_gaussian
python cli.py --help
python cli.py run --help
python cli.py list --help
python cli.py compare --help
python cli.py export --help
python cli.py diagnose --help
grep "quickstart.md" README.md
grep "pip install -e mace_ML_pkg" README.md
```
</verification>

<success_criteria>
- All five CLI subcommands have accurate, non-misleading --help output
- `compare` and `export` say "Not yet implemented" with actual alternatives
- `diagnose` mentions Gaussian 16 and CUDA checks
- `run` shows --output-dir in examples
- README links to docs/quickstart.md
- README Installation section (5-step procedure) is intact
- ruff passes on mace_gaussian/cli.py
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-01-SUMMARY.md`
</output>
