---
phase: 10-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mace_gaussian/cli.py
autonomous: true
requirements:
  - DOC-04

must_haves:
  truths:
    - "python cli.py compare water prints 'Not yet implemented' and references run_analysis.py"
    - "python cli.py export water prints 'Not yet implemented' and references comparison_results/"
    - "Neither compare nor export runtime output contains 'COMING SOON' or 'Planned features:'"
  artifacts:
    - path: "mace_gaussian/cli.py"
      provides: "compare() and export() bodies replaced with honest 'Not yet implemented' output"
---

<objective>
Fix the runtime bodies of the `compare` and `export` CLI commands so their actual output
matches their docstrings.

Purpose: Plan 01 correctly updated the docstrings and `--help` text for `compare` and
`export` to say "Not yet implemented." However, the function bodies that execute when
the commands are actually invoked were not updated. A user running `python cli.py compare
water` (without `--help`) still sees a misleading "COMING SOON" banner and a "Planned
features:" list that contradicts the accurate `--help` text. This gap-closure plan
removes those four `click.echo()` call blocks and replaces them with a single honest
message that mirrors the docstring.

Output: `compare()` and `export()` bodies that print a concise "Not yet implemented"
message consistent with the docstring, with no "COMING SOON" or "Planned features:" text.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-documentation/10-CONTEXT.md

<interfaces>
<!-- compare() body — current state (lines 291-302 of mace_gaussian/cli.py): -->
    click.echo(f"\n{'=' * 60}")
    click.echo("COMPARE COMMAND - COMING SOON")
    click.echo(f"{'=' * 60}\n")
    click.echo(f"This command will compare results for: {molecule}")
    click.echo(f"Results directory: {output_dir}")
    click.echo("\nPlanned features:")
    click.echo("  - Energy comparison table")
    click.echo("  - Frequency comparison (harmonic vs anharmonic)")
    click.echo("  - Runtime analysis")
    click.echo("  - Visualization plots")
    click.echo("\nTo implement this feature, add analysis code to a new module.")
    click.echo(f"{'=' * 60}\n")

<!-- export() body — current state (lines 333-349 of mace_gaussian/cli.py): -->
    if output is None:
        output = f"{molecule}_results.{format}"

    click.echo(f"\n{'=' * 60}")
    click.echo("EXPORT COMMAND - COMING SOON")
    click.echo(f"{'=' * 60}\n")
    click.echo(f"Molecule: {molecule}")
    click.echo(f"Format: {format}")
    click.echo(f"Output: {output}")
    click.echo(f"Results directory: {results_dir}")
    click.echo("\nPlanned features:")
    click.echo("  - Export all frequencies to CSV/Excel")
    click.echo("  - Export energies and dipole moments")
    click.echo("  - Export metadata (runtimes, parameters)")
    click.echo("  - JSON export for programmatic access")
    click.echo("\nTo implement this feature, add export code to a new module.")
    click.echo(f"{'=' * 60}\n")

<!-- Target for compare() body: -->
    click.echo(
        "Not yet implemented. To analyze and compare results, use:\n\n"
        "    python run_analysis.py <molecule>           (anharmonic: fundamentals, overtones, combinations)\n"
        "    python run_analysis_harmonic.py <molecule>  (harmonic: fundamentals with mode matching)\n\n"
        "These scripts generate comparison plots, regression analysis, and an HTML report\n"
        "in the analysis_results/ directory."
    )

<!-- Target for export() body: -->
    click.echo(
        "Not yet implemented. Calculation results are stored as JSON files under\n"
        f"comparison_results/{molecule}/<energy_calc>_<dipole_calc>/results.json."
    )
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace compare() body with honest output</name>
  <files>mace_gaussian/cli.py</files>
  <action>
    In `mace_gaussian/cli.py`, find the `compare()` function body — the block of
    `click.echo()` calls starting with `click.echo(f"\n{'=' * 60}")` and ending with
    `click.echo(f"{'=' * 60}\n")` (lines ~291-302, immediately after the docstring).

    Replace that entire block with a single `click.echo()` call that mirrors the docstring:

    ```python
    click.echo(
        "Not yet implemented. To analyze and compare results, use:\n\n"
        "    python run_analysis.py <molecule>           (anharmonic: fundamentals, overtones, combinations)\n"
        "    python run_analysis_harmonic.py <molecule>  (harmonic: fundamentals with mode matching)\n\n"
        "These scripts generate comparison plots, regression analysis, and an HTML report\n"
        "in the analysis_results/ directory."
    )
    ```

    Do NOT touch the docstring, the `@cli.command()` decorator, the `@click.argument` /
    `@click.option` decorators, or any other function. Only replace the body echo block.

    After editing, run:
        ruff check --fix mace_gaussian/cli.py && ruff format mace_gaussian/cli.py
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian && python cli.py compare water 2>&1 | grep -c "COMING SOON\|Planned features" ; python cli.py compare water 2>&1 | grep "Not yet implemented"</automated>
  </verify>
  <done>
    `python cli.py compare water` output contains "Not yet implemented" and does NOT
    contain "COMING SOON" or "Planned features:".
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace export() body with honest output</name>
  <files>mace_gaussian/cli.py</files>
  <action>
    In `mace_gaussian/cli.py`, find the `export()` function body — keep the existing
    `if output is None:` guard (line ~333-334) but replace the entire block of
    `click.echo()` calls starting with `click.echo(f"\n{'=' * 60}")` and ending with
    `click.echo(f"{'=' * 60}\n")` (lines ~336-349).

    Replace only the echo block (leave the `if output is None:` assignment in place) with:

    ```python
    click.echo(
        "Not yet implemented. Calculation results are stored as JSON files under\n"
        f"comparison_results/{molecule}/<energy_calc>_<dipole_calc>/results.json."
    )
    ```

    Do NOT touch the docstring, decorators, or the `if output is None:` guard. Only
    replace the banner/planned-features echo block.

    After editing, run:
        ruff check --fix mace_gaussian/cli.py && ruff format mace_gaussian/cli.py
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian && python cli.py export water 2>&1 | grep -c "COMING SOON\|Planned features" ; python cli.py export water 2>&1 | grep "Not yet implemented"</automated>
  </verify>
  <done>
    `python cli.py export water` output contains "Not yet implemented" and does NOT
    contain "COMING SOON" or "Planned features:".
  </done>
</task>

</tasks>

<verification>
Run both commands without --help and confirm the output is honest and concise:

```bash
cd /home/mot/mace_gaussian

# Must NOT contain misleading text
python cli.py compare water 2>&1 | grep -E "COMING SOON|Planned features"   # must return empty
python cli.py export water 2>&1 | grep -E "COMING SOON|Planned features"    # must return empty

# Must contain honest message
python cli.py compare water 2>&1 | grep "Not yet implemented"   # must match
python cli.py export water 2>&1 | grep "Not yet implemented"    # must match

# --help output must be unchanged from Plan 01 fixes
python cli.py compare --help 2>&1 | grep "Not yet implemented"
python cli.py export --help 2>&1 | grep "Not yet implemented"

# Linting must pass
ruff check mace_gaussian/cli.py
```
</verification>

<success_criteria>
- `python cli.py compare water` prints "Not yet implemented" with run_analysis.py reference
- `python cli.py export water` prints "Not yet implemented" with comparison_results/ path reference
- Neither runtime invocation contains "COMING SOON" or "Planned features:"
- The `--help` output for both commands is unchanged (still shows "Not yet implemented")
- ruff passes on mace_gaussian/cli.py with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-04-SUMMARY.md`
</output>
