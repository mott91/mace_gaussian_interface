---
phase: 01-narrative-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - presentation/generate_pptx_v2.py
autonomous: true
requirements:
  - NARR-02

must_haves:
  truths:
    - "slide_status() tells a physics/chemistry research journey: tool built → benchmark run → frequencies solved, intensities model-dependent → anharmonic + 25-mol campaign next"
    - "slide_status() does not read like a git version history or CI checklist"
    - "slide_motivation() has fewer bullets per section with stronger signal, keeps Problem/Solution/Impact structure, and removes verbose secondary bullets"
    - "The PPTX regenerates cleanly with all 11 slides"
  artifacts:
    - path: "presentation/generate_pptx_v2.py"
      provides: "Final Phase 1 presentation with rewritten status and tightened motivation"
      contains: "slide_status, slide_motivation"
  key_links:
    - from: "slide_status left column"
      to: "research narrative spine"
      via: "git log commits"
      pattern: "tool built.*benchmark.*frequencies.*intensities.*anharmonic"
    - from: "slide_motivation"
      to: "Problem/Solution/Impact sections"
      via: "content_slide lines"
      pattern: "Problem.*Solution.*Impact"
---

<objective>
Rewrite slide_status() as a physics/chemistry research journey narrative and tighten slide_motivation() for signal-to-noise. These are the two remaining slide-level content changes for Phase 1.

Purpose: The status slide must land as "here's what we learned" not "here are the commits." The motivation slide must hook the audience fast.
Output: presentation/generate_pptx_v2.py with rewritten slide_status() and tightened slide_motivation().
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-narrative-structure/01-CONTEXT.md
@.planning/phases/01-narrative-structure/01-01-SUMMARY.md

<interfaces>
<!-- Slide-building functions (unchanged from Plan 01) -->

```python
def content_slide(prs, command, lines: list[tuple[str, RGBColor]]) -> slide
def two_col_slide(prs, command, left_lines, right_lines, split=4.3) -> slide
```

Color constants: BG, TEXT, ACCENT, GREEN, YELLOW, DIM, RED

Current slide_status() uses two_col_slide with command "$ git log --oneline --graph".
Keep that command. Replace the content entirely.

Current slide_motivation() uses content_slide with command "$ cat motivation.md".
Keep that command. Tighten the content.

Spine for slide_status (from CONTEXT.md):
  "tool built → benchmark run → frequencies solved, intensities model-dependent
   → anharmonic + 25-mol campaign next"

Key finding to surface:
  frequencies: all 8 combos R² > 0.999 for water (harmonic)
  intensities: mace_ml dipole wins (~0.72) over espaloma (~0.52) — dipole model dominates
  anharmonic: pipeline exists, validation ongoing
  next: 25-molecule systematic campaign
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite slide_status as research journey</name>
  <files>presentation/generate_pptx_v2.py</files>
  <action>
Replace the content of slide_status() entirely. Keep two_col_slide and keep the command "$ git log --oneline --graph".

The new slide tells a narrative using git log as a metaphor — each "commit" is a research milestone, not an actual git sha. Left column is the story arc, right column is "what this means" and what's next.

Left column — the journey (use git log visual idiom):
  Section header: "# What we built and found" in ACCENT
  Blank line.
  Entries styled like git log (use DIM for the graph chars, TEXT or GREEN for the message):
    "* built: ZMQ bridge — Gaussian + ML in real time" (GREEN)
    "* built: 4 energy × 2 dipole calculators (8 combos)" (TEXT)
    "* built: mode matching via eigenvector dot product" (TEXT)
    "* ran:   harmonic benchmark — water, aspirin, glycine, …" (TEXT)
    "* found: frequencies: R² > 0.999 across all combos" (GREEN)
    "* found: intensities: dipole model dominates (mace_ml > espaloma)" (YELLOW)
    "* open:  anharmonic VPT2 validation (pipeline exists)" (DIM)
    "* open:  25-molecule systematic campaign" (DIM)

Right column — the meaning:
  Section header: "# The thesis question" in ACCENT
  Blank line.
  "Does energy surface quality or" (TEXT)
  "dipole model quality dominate IR accuracy?" (TEXT)
  Blank.
  "Early signal: dipole model matters more" (YELLOW)
  "for intensities; energy model drives" (TEXT)
  "frequency accuracy." (TEXT)
  Blank.
  Section header: "# What's next" in ACCENT
  "→ Anharmonic: overtones + combination bands" (TEXT)
  "→ 25-molecule benchmark across" (TEXT)
  "  functional groups + sizes" (TEXT)
  "→ $ mace-gaussian run molecule.xyz" (GREEN)

Keep split=5.1.
  </action>
  <verify>
    <automated>python3 -c "import ast; src=open('/home/mot/mace_gaussian_march26_presentation/mace_gaussian/presentation/generate_pptx_v2.py').read(); ast.parse(src); assert 'thesis question' in src.lower() or 'What we built' in src; print('syntax OK, status content updated')"</automated>
  </verify>
  <done>slide_status() tells a research journey (built → found → open) with thesis question framing. No git sha hashes, no CI status, no version numbers. Running python3 presentation/generate_pptx_v2.py succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten slide_motivation</name>
  <files>presentation/generate_pptx_v2.py</files>
  <action>
Tighten slide_motivation(). Keep content_slide and "$ cat motivation.md" command. Keep Problem/Solution/Impact structure. Cut secondary bullets that restate the primary one. Each section should have 1 strong primary bullet + at most 1 supporting line. Aim for ~10 lines total (down from 15).

Suggested tight version:
  ("# Problem", ACCENT),
  ("  DFT frequency calculations: slow, expensive, hard to scale", TEXT),
  ("  → bottleneck for any systematic IR benchmark campaign", DIM),
  ("", DIM),
  ("# Solution", ACCENT),
  ("  Hybrid ML-QM: MACE energy + dedicated dipole model,", TEXT),
  ("  injected into Gaussian 16 via ZMQ bridge — no Gaussian hacks", TEXT),
  ("", DIM),
  ("# Impact", ACCENT),
  ("  → Systematic benchmark: which ML combo gives best IR?", TEXT),
  ("  → Thesis question: energy surface vs dipole model quality", TEXT),
  ("  → 10-100× faster than pure DFT for dipole-heavy steps", GREEN),

Feel free to adjust wording for punch and clarity, but keep the three-section structure and the terminal idiom.

After editing, run the generator to confirm the output file regenerates:
  cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian && python3 presentation/generate_pptx_v2.py
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian && python3 presentation/generate_pptx_v2.py && python3 -c "from pptx import Presentation; prs=Presentation('presentation/presentation_v2.pptx'); assert len(prs.slides)==11, f'expected 11 slides, got {len(prs.slides)}'; print(f'OK: {len(prs.slides)} slides')"</automated>
  </verify>
  <done>slide_motivation() has 3 tight sections (Problem/Solution/Impact) with at most 2 lines each, no verbose secondary bullets. PPTX generates cleanly with 11 slides.</done>
</task>

</tasks>

<verification>
Final check — all Phase 1 success criteria:
1. Slide order: Title → Motivation → IR theory → Architecture → Dipole methods → ZMQ → Mode matching → Results A → Results B → Status → Questions
2. slide_status reads as a research journey with physics/chemistry findings
3. VPT2/anharmonic framed as ongoing context at the end of the IR theory slide (done in Plan 01)

```bash
cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian
python3 presentation/generate_pptx_v2.py
```
Output should print "11 slides" and produce presentation/presentation_v2.pptx.
</verification>

<success_criteria>
- slide_status() uses "built/found/open" narrative arc with thesis question framing
- slide_motivation() is tighter: 3 sections, ~10 lines, no redundant secondary bullets
- python3 presentation/generate_pptx_v2.py runs without errors
- presentation/presentation_v2.pptx exists with exactly 11 slides
- No deferred items (color-coding, slide numbers, speaking notes) are included
</success_criteria>

<output>
After completion, create `.planning/phases/01-narrative-structure/01-02-SUMMARY.md`
</output>
