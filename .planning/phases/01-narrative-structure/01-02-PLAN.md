---
phase: 01-narrative-structure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - presentation/generate_pptx_v2.py
autonomous: true
requirements:
  - NARR-01
  - NARR-02
  - NARR-03

must_haves:
  truths:
    - "IR theory slide is computationally framed: harmonic approx → normal modes → why you need energy + dipole derivatives"
    - "IR theory slide ends with 2 lines on anharmonic/VPT2 as ongoing work, not a broken feature"
    - "Motivation slide keeps Problem/Solution/Impact structure but has fewer bullets (verbose secondary bullets removed)"
    - "Motivation slide keeps the thesis question line (load-bearing for story arc)"
    - "Status slide reads as research journey: what we built → what we ran → what we found → what's open"
    - "Status slide contains no git task checklist language (no 'CI on every push', no 'PubChem fetcher')"
    - "Script still exits 0 and generates 11 slides after all rewrites"
  artifacts:
    - path: "presentation/generate_pptx_v2.py"
      provides: "Content-rewritten slide generator"
      contains: "slide_status"
  key_links:
    - from: "slide_ir_theory"
      to: "slide_architecture"
      via: "anharmonic footer lines set up the 'what ML provides' segue"
      pattern: "Anharmonic.*VPT2.*ongoing"
    - from: "slide_status left panel"
      to: "slide_status right panel"
      via: "what we found leads into thesis question"
      pattern: "Thesis question"
---

<objective>
Rewrite three existing slide functions in generate_pptx_v2.py: slide_ir_theory (computational reframe), slide_motivation (tighten bullets), and slide_status (physics/chemistry journey narrative). No structural changes — Plan 01 already fixed the structure.

Purpose: Lock the story arc so each slide earns its place. The IR slide sets up the architecture. The motivation slide leads with the right problem. The status slide closes the loop with honest science, not a git task list.
Output: generate_pptx_v2.py with three rewritten slide functions, still generating 11 slides cleanly.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-narrative-structure/01-CONTEXT.md
@.planning/phases/01-narrative-structure/01-RESEARCH.md
@.planning/phases/01-narrative-structure/01-01-SUMMARY.md

<interfaces>
<!-- Patterns established in Plan 01 — executor uses these directly. -->

Color constants:
```python
BG, TEXT, ACCENT, GREEN, YELLOW, DIM, RED  # all available
FONT = "Consolas"
```

Layout functions (do NOT change these):
```python
def content_slide(prs, command, lines): ...   # lines = list of (text, color) tuples, Pt(14)
def two_col_slide(prs, command, left_lines, right_lines, split=4.3): ...  # Pt(13) per panel
```

Functions to REWRITE (replace their entire body):
- slide_motivation(prs)   — currently verbose, keep Problem/Solution/Impact structure
- slide_ir_theory(prs)    — currently IR-101 framing, reframe as computational
- slide_status(prs)       — currently git-task-list framing, reframe as research journey

Functions to NOT TOUCH:
- slide_title, slide_architecture, slide_dipole_methods, slide_zmq,
  slide_mode_matching, slide_results_overview, slide_results_table, slide_questions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite slide_ir_theory as computational framing + anharmonic footer</name>
  <files>presentation/generate_pptx_v2.py</files>
  <action>
Replace the entire body of slide_ir_theory(prs) with a two-column layout:

Left panel: "What Gaussian needs" — the computational pipeline framing.
Right panel: "What ML provides" — where MACE fits in.

End the left panel with 2-3 lines on anharmonic/VPT2 as ongoing direction
(per locked decision: frame it as "next step", not broken).

Use this content exactly (adjust minor wording at discretion, but hit all
the key concepts):

```python
def slide_ir_theory(prs):
    """Slide 2: What the software computes — computational framing."""
    two_col_slide(prs, "$ man ir_spectroscopy",
        left_lines=[
            ("# What the software computes", ACCENT),
            ("", DIM),
            ("  Harmonic approximation:", TEXT),
            ("  V(x) \u2248 V\u2080 + \u00bdk\u00b7x\u00b2", TEXT),
            ("", DIM),
            ("  \u2192 Diagonalise Hessian \u2192 normal modes", TEXT),
            ("  \u2192 3N\u22126 frequencies \u03c9\u1d62", TEXT),
            ("  \u2192 Intensities \u221d |\u0394\u03bc/\u0394Q|\u00b2", TEXT),
            ("", DIM),
            ("  For each displaced geometry:", TEXT),
            ("  \u2192 energy (forces \u2192 Hessian)", TEXT),
            ("  \u2192 dipole \u03bc\u20d7 (intensity)", TEXT),
            ("  \u2192 Gaussian displaces ~hundreds of times", TEXT),
            ("", DIM),
            ("# Anharmonic (VPT2): ongoing work", ACCENT),
            ("  \u2192 Already running. Overtones + combination bands.", TEXT),
            ("  \u2192 Results pending full validation \u2014 next direction.", DIM),
        ],
        right_lines=[
            ("# What ML provides", ACCENT),
            ("", DIM),
            ("  Each displaced geometry \u2192 ML query:", TEXT),
            ("", DIM),
            ("  [geometry]", GREEN),
            ("      \u2193  MACE energy model", TEXT),
            ("  E, F, Hessian contribution", DIM),
            ("", DIM),
            ("  [geometry]", GREEN),
            ("      \u2193  MACE dipole model", TEXT),
            ("  \u03bc\u20d7 = (\u03bc_x, \u03bc_y, \u03bc_z)", DIM),
            ("", DIM),
            ("  Both injected into Gaussian", TEXT),
            ("  via ZMQ bridge \u2014 next slide.", TEXT),
            ("", DIM),
            ("  IR range: 400\u20134000 cm\u207b\u00b9", DIM),
            ("  1 cm\u207b\u00b9 \u2248 0.124 meV", DIM),
        ],
        split=5.0,
    )
```

The unicode escapes: \u2248 = ≈, \u00bd = ½, \u00b7 = ·, \u2192 = →,
\u00b2 = ², \u03c9 = ω, \u1d62 = subscript i, \u221d = ∝, \u0394 = Δ,
\u03bc = μ, \u20d7 = vector arrow above, \u2014 = em dash,
\u2193 = ↓, \u207b\u00b9 = superscript -1.

Use either unicode escapes or literal characters — be consistent within the function.
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian && source .venv/bin/activate && python -c "import ast; ast.parse(open('presentation/generate_pptx_v2.py').read()); print('syntax OK')" && python -c "src=open('presentation/generate_pptx_v2.py').read(); assert 'What the software computes' in src, 'IR reframe missing'; assert 'Anharmonic' in src, 'anharmonic footer missing'; print('IR theory reframe OK')"</automated>
  </verify>
  <done>slide_ir_theory body replaced with two-column computational framing. Left panel covers harmonic approx → normal modes → displaced geometry loop. Right panel shows ML's role (energy + dipole per geometry). Footer adds 2 lines on anharmonic as ongoing direction. File parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten slide_motivation and rewrite slide_status as research journey</name>
  <files>presentation/generate_pptx_v2.py</files>
  <action>
Two rewrites in one task — both are content replacements on existing functions.

REWRITE A: slide_motivation — tighten bullets, keep Problem/Solution/Impact,
KEEP the thesis question line (it is load-bearing for the story arc).
Remove verbose secondary bullets. The current function has 15 lines; target ~12.

Replace body with:
```python
def slide_motivation(prs):
    """Slide 1: Why we built this."""
    content_slide(prs, "$ cat motivation.md", [
        ("# Problem", ACCENT),
        ("  \u2192 DFT frequency calculations: slow (minutes to hours per molecule)", TEXT),
        ("  \u2192 Anharmonic VPT2: even more expensive \u2014 scales as O(N\u2074)", TEXT),
        ("  \u2192 Bottleneck for any systematic benchmark campaign", TEXT),
        ("", DIM),
        ("# Solution", ACCENT),
        ("  \u2192 Hybrid ML-QM: ML computes dipole derivatives in real-time", TEXT),
        ("  \u2192 ZMQ bridge injects results into Gaussian \u2014 no modifications needed", TEXT),
        ("  \u2192 10\u2013100\u00d7 faster than pure DFT for dipole-heavy steps", TEXT),
        ("", DIM),
        ("# Impact", ACCENT),
        ("  \u2192 Rapid IR predictions across chemical space", TEXT),
        ("  \u2192 Thesis question: energy surface vs dipole model quality", TEXT),
    ])
```

REWRITE B: slide_status — replace the git-task-list content with the
research journey narrative. Use two_col_slide, keep the
`$ git log --oneline --graph` command prompt (locked decision).

Left panel: what we built → what we ran → what we found.
Right panel: what's still open + thesis question.

Replace body with:
```python
def slide_status(prs):
    """Slide 9: Research journey — what we built, ran, found, and what's next."""
    two_col_slide(prs, "$ git log --oneline --graph",
        left_lines=[
            ("# What we built", ACCENT),
            ("  ZMQ bridge: ML dipoles \u2192 Gaussian VPT2", TEXT),
            ("  CLI: mace-gaussian run molecule.xyz", DIM),
            ("  4 energy \u00d7 2 dipole models = 8 combinations", TEXT),
            ("", DIM),
            ("# What we ran", ACCENT),
            ("  Harmonic benchmark across 8 molecules", TEXT),
            ("  Water, aspirin, glycine, methane, ethane, NH\u2083, CO", DIM),
            ("", DIM),
            ("# What we found", ACCENT),
            ("  Frequencies: R\u00b2 > 0.999 across all combos \u2713", GREEN),
            ("  Intensities: mace_ml >> espaloma (0.72 vs 0.52)", GREEN),
            ("  Speedup scales with molecule size (aspirin: ~18\u00d7)", GREEN),
        ],
        right_lines=[
            ("# Still open", ACCENT),
            ("", DIM),
            ("  Anharmonic validation:", TEXT),
            ("  \u2192 VPT2 pipeline runs; accuracy TBD", DIM),
            ("  \u2192 Overtones + combination bands", DIM),
            ("", DIM),
            ("  25-molecule benchmark campaign:", TEXT),
            ("  \u2192 Systematic across functional groups", DIM),
            ("  \u2192 Batch runner in progress", DIM),
            ("", DIM),
            ("# Thesis question", ACCENT),
            ("  Energy surface quality", TEXT),
            ("  vs dipole model quality \u2014", TEXT),
            ("  which dominates IR accuracy?", TEXT),
            ("", DIM),
            ("  $ mace-gaussian run molecule.xyz", GREEN),
        ],
        split=5.1,
    )
```

After both rewrites, run the script to confirm 11 slides still generate.
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian && source .venv/bin/activate && python presentation/generate_pptx_v2.py && python -c "
from pptx import Presentation
p = Presentation('presentation/presentation_v2.pptx')
assert len(p.slides) == 11, f'Expected 11 slides, got {len(p.slides)}'
print(f'OK: {len(p.slides)} slides')
" && python -c "
src = open('presentation/generate_pptx_v2.py').read()
assert 'Thesis question: energy surface vs dipole model quality' in src or 'energy surface vs dipole model quality' in src, 'thesis question missing'
assert 'What we built' in src, 'status journey missing'
assert 'What the software computes' in src, 'IR reframe missing'
assert '131 tests' not in src, 'old git-task language still present'
assert 'CI on every push' not in src, 'old CI line still present'
print('content checks OK')
"</automated>
  </verify>
  <done>slide_motivation has tighter bullets with thesis question preserved. slide_status reads as physics/chemistry research journey (what we built/ran/found/open). No git task checklist language remains. Script generates 11 slides without error.</done>
</task>

</tasks>

<verification>
Full phase gate — run after both tasks:

```bash
cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian
source .venv/bin/activate

# Script runs clean
python presentation/generate_pptx_v2.py

# Slide count
python -c "
from pptx import Presentation
p = Presentation('presentation/presentation_v2.pptx')
print(f'Slides: {len(p.slides)}')
assert len(p.slides) == 11
print('PASS: 11 slides')
"

# Content checks
python -c "
src = open('presentation/generate_pptx_v2.py').read()
checks = [
    ('def slide_vpt2' not in src, 'slide_vpt2 deleted'),
    ('slide_results_overview' in src, 'results overview exists'),
    ('slide_results_table' in src, 'results table exists'),
    ('What the software computes' in src, 'IR reframe present'),
    ('Anharmonic' in src, 'anharmonic footer present'),
    ('What we built' in src, 'status journey present'),
    ('Thesis question' in src, 'thesis question present'),
    ('131 tests' not in src, 'git task language removed'),
    ('presentation/presentation_v2.pptx' in src, 'output path fixed'),
    ('/home/mot/mace_gaussian/' not in src, 'old path removed'),
]
for ok, label in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'  {status}: {label}')
all_ok = all(ok for ok, _ in checks)
assert all_ok, 'Some checks failed'
print('ALL CHECKS PASSED')
"
```
</verification>

<success_criteria>
- python presentation/generate_pptx_v2.py exits 0 with no exceptions
- Generated .pptx has exactly 11 slides
- slide_ir_theory: left panel covers harmonic approx → normal modes → displaced geometry loop; ends with anharmonic as "ongoing work" (not broken)
- slide_motivation: 3 sections (Problem/Solution/Impact), ~12 lines total, thesis question line preserved
- slide_status: left panel tells what-we-built/ran/found; right panel covers open questions and thesis question; no CI/git-task-list language
- All other slides (title, architecture, dipole methods, ZMQ, mode matching, results overview, results table, questions) are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01-narrative-structure/01-02-SUMMARY.md`
</output>
