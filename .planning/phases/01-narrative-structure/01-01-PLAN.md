---
phase: 01-narrative-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - presentation/generate_pptx_v2.py
autonomous: true
requirements:
  - NARR-01
  - NARR-03

must_haves:
  truths:
    - "Slide deck has 11 slides in the correct order: Title, Motivation, IR theory, Architecture, Dipole methods, ZMQ bridge, Mode matching, Results A (ls results/), Results B (comparison table), Status/journey, Questions"
    - "The standalone VPT2 slide function is removed from main()"
    - "IR theory slide covers computational framing: harmonic approximation, normal modes, energy + dipole derivatives, with 1-2 lines on VPT2/anharmonic as future direction"
    - "Results A slide uses terminal idiom ($ ls results/) and references the HTML report artifacts"
    - "Results B slide shows all 8 calculator combos with harmonic metrics, frequencies and intensities in separate columns"
    - "Output path in main() points to presentation/presentation_v2.pptx (relative or correct absolute path)"
  artifacts:
    - path: "presentation/generate_pptx_v2.py"
      provides: "Updated presentation generator with 11 slides in correct order"
      contains: "slide_results_ls, slide_results_table"
  key_links:
    - from: "main()"
      to: "slide functions"
      via: "ordered function calls"
      pattern: "slide_title.*slide_motivation.*slide_ir_theory.*slide_architecture.*slide_dipole_methods.*slide_zmq.*slide_mode_matching.*slide_results_ls.*slide_results_table.*slide_status.*slide_questions"
---

<objective>
Restructure the presentation source: remove the VPT2 slide, reframe the IR theory slide computationally, add two new results slides, fix slide order in main(), and fix the output path.

Purpose: Lock the 11-slide structure so subsequent content work and status rewrite happen in the correct frame.
Output: presentation/generate_pptx_v2.py with correct slide count, order, and new slide functions.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-narrative-structure/01-CONTEXT.md

<interfaces>
<!-- Key slide-building functions available in generate_pptx_v2.py -->

Color constants: BG, TEXT, ACCENT, GREEN, YELLOW, DIM, RED
FONT = "Consolas"

```python
def blank_slide(prs) -> slide
def add_footer(slide, text=DATE)
def add_prompt(slide, command, y=0.35)
def content_slide(prs, command, lines: list[tuple[str, RGBColor]]) -> slide
def two_col_slide(prs, command, left_lines, right_lines, split=4.3) -> slide
```

Lines are (text, RGBColor) tuples. Section headers use ACCENT, body uses TEXT,
good results GREEN, warnings YELLOW, subdued/empty DIM.

Current slide order in main() (12 slides):
  slide_title, slide_motivation, slide_ir_theory, slide_vpt2,
  slide_architecture, slide_dipole_methods, slide_zmq,
  slide_mode_matching, slide_results_water, slide_results_aspirin,
  slide_status, slide_questions

Target slide order (11 slides):
  0. slide_title           — unchanged
  1. slide_motivation      — unchanged (tightening deferred to Plan 02)
  2. slide_ir_theory       — REFRAME (see Task 1)
  3. slide_architecture    — unchanged
  4. slide_dipole_methods  — unchanged
  5. slide_zmq             — unchanged
  6. slide_mode_matching   — unchanged
  7. slide_results_ls      — NEW ($ ls results/)
  8. slide_results_table   — NEW (comparison table)
  9. slide_status          — unchanged body for now (rewritten in Plan 02)
  10. slide_questions       — unchanged

Water harmonic metrics (from analysis_results_harmonic/water/data/metrics_summary.json):
  mace_anicc + mace_ml:   MAE  19.0  R²_freq 1.0000  R²_int 0.719
  mace_anicc + espaloma:  MAE  19.0  R²_freq 1.0000  R²_int 0.518
  mace_off   + mace_ml:   MAE  20.7  R²_freq 0.9999  R²_int 0.719
  mace_omol  + mace_ml:   MAE  22.7  R²_freq 1.0000  R²_int 0.719
  mace_off   + espaloma:  MAE  20.7  R²_freq 0.9999  R²_int 0.516
  mace_omol  + espaloma:  MAE  22.7  R²_freq 1.0000  R²_int 0.521
  mace_mp    + mace_ml:   MAE 106.3  R²_freq 1.0000  R²_int 0.718
  mace_mp    + espaloma:  MAE 106.3  R²_freq 1.0000  R²_int 0.514

HTML reports exist at: analysis_results_harmonic/{molecule}/report.html
Molecules with harmonic results: water, aspirin, gly, ammonia, CH4_ase, co, cocaine, bh3_nh3, C2H6_ase
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe slide_ir_theory and remove slide_vpt2</name>
  <files>presentation/generate_pptx_v2.py</files>
  <action>
Replace the body of slide_ir_theory() with a computational framing: "what the software needs to compute frequencies and intensities." Keep it two-column. The new content should cover:

Left column — the computational pipeline:
- Section header: "# What the software computes"
- Harmonic approximation: displace geometry → compute energy + forces
- Normal modes from the Hessian (2nd derivative matrix of energy)
- Intensities from dipole derivatives dμ/dQ for each mode
- Each displaced geometry needs: energy (from MACE) + dipole (from MACE4IR/Espaloma)
- Gaussian orchestrates hundreds of displacements, asks external program each time

Right column — the physics (brief) + anharmonic as future direction:
- Section header: "# The harmonic approximation"
- V(x) ≈ V₀ + ½k·x² — works well for fundamentals
- Normal modes: 3N-6 independent vibrations
- Intensities ∝ (dμ/dQ)² — dipole derivative matters
- Brief note that harmonic is what this benchmark uses
- 1-2 lines at end (use DIM or YELLOW): anharmonic/VPT2 extends this — cubic/quartic terms, overtones; pipeline supports it, validation ongoing

Keep the prompt as "$ man ir_spectroscopy" or change to "$ python3 -c 'help(ir_theory)'" — use your judgment for terminal feel.

Delete the entire slide_vpt2() function.

In main(), remove the call to slide_vpt2(prs) — it currently sits between slide_ir_theory and slide_architecture.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('presentation/generate_pptx_v2.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>slide_ir_theory() has computational framing (Hessian, dipole derivatives, external displacements), ends with VPT2/anharmonic as future direction. slide_vpt2() does not exist in the file. main() does not call slide_vpt2.</done>
</task>

<task type="auto">
  <name>Task 2: Add slide_results_ls and slide_results_table; replace old results slides; fix output path</name>
  <files>presentation/generate_pptx_v2.py</files>
  <action>
Delete slide_results_water() and slide_results_aspirin() functions entirely.

Add two new slide functions:

**slide_results_ls(prs):**
Use content_slide(). Command: "$ ls analysis_results_harmonic/". Content should feel like a terminal directory listing. Show molecules as files/dirs with a brief annotation. Then call out the HTML report artifact — something like:
  - water/          → 3 modes, H₂O
  - gly/            → 27 modes, glycine
  - aspirin/        → 51 modes, C₉H₈O₄
  - ammonia/        → 6 modes, NH₃
  - cocaine/        → 96 modes, C₁₇H₂₁NO₄
  (BH₃·NH₃ excluded — future work)
Then show the report anchor: "→ each directory: report.html (spectrum + regression + metrics)" in GREEN or ACCENT. One line on what the HTML report shows (spectrum comparison, regression plot, mode overlap). This slide says "here are real results, the artifact is the HTML report."

**slide_results_table(prs):**
Use two_col_slide(). Command: "$ cat harmonic_benchmark.md". Show the 8 combinations in a ranked table. Left column: frequencies (MAE, R²). Right column: intensities (R²). Use real water data from the metrics_summary.json above.

Left column header: "# Frequencies (harmonic, H₂O)"
Table rows ranked by MAE, format:
  combo                 MAE    R²_freq
  mace_anicc + mace_ml   19  1.0000
  mace_anicc + espaloma  19  1.0000
  mace_off   + mace_ml   21  0.9999
  mace_omol  + mace_ml   23  1.0000
  mace_off   + espaloma  21  0.9999
  mace_omol  + espaloma  23  1.0000
  mace_mp    + mace_ml  106  1.0000
  mace_mp    + espaloma 106  1.0000

Right column header: "# Intensities (R²_intensity)"
Show the R²_intensity values for the same combos, same order:
  mace_ml dipole:   R² ≈ 0.72 across combos  (use GREEN for 0.72)
  espaloma dipole:  R² ≈ 0.52 across combos  (use YELLOW for 0.52)
Include a takeaway: "→ Frequencies: all combos within ~25 cm⁻¹ (mace_mp outlier)" and "→ Intensities: dipole model dominates — mace_ml > espaloma". Use TEXT/DIM for labels, GREEN for good values, YELLOW for moderate.

In main(), remove slide_results_water(prs) and slide_results_aspirin(prs) calls. Add slide_results_ls(prs) and slide_results_table(prs) in their place (positions 7 and 8 in the final order).

Fix the output path: change the hardcoded path in main() from "/home/mot/mace_gaussian/presentation/presentation_v2.pptx" to "presentation/presentation_v2.pptx" (relative path, works from repo root).

Update the docstring at top of file: change "12 slides" to "11 slides".
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian && python3 -c "import ast; src=open('presentation/generate_pptx_v2.py').read(); ast.parse(src); assert 'slide_results_ls' in src; assert 'slide_results_table' in src; assert 'slide_results_water' not in src; assert 'slide_results_aspirin' not in src; assert 'slide_vpt2' not in src; assert 'presentation/presentation_v2.pptx' in src; print('all checks pass')"</automated>
  </verify>
  <done>slide_results_ls() and slide_results_table() exist with real water metrics. slide_results_water() and slide_results_aspirin() are gone. main() calls slides in the correct 11-slide order. Output path is "presentation/presentation_v2.pptx". PPTX generates without error: python3 presentation/generate_pptx_v2.py succeeds and produces presentation/presentation_v2.pptx.</done>
</task>

</tasks>

<verification>
Run the presentation generator and verify output:
```bash
cd /home/mot/mace_gaussian_march26_presentation/mace_gaussian
python3 presentation/generate_pptx_v2.py
ls -la presentation/presentation_v2.pptx
```
Should produce the file without errors, print "11 slides".

Verify slide count by importing:
```python
from pptx import Presentation
prs = Presentation("presentation/presentation_v2.pptx")
assert len(prs.slides) == 11
```
</verification>

<success_criteria>
- presentation/generate_pptx_v2.py parses cleanly (no syntax errors)
- 11 slides in correct order confirmed by running the generator
- slide_vpt2, slide_results_water, slide_results_aspirin are gone
- slide_ir_theory has computational framing + 1-2 VPT2/anharmonic lines
- slide_results_ls shows molecule listing with HTML report reference
- slide_results_table shows ranked 8-combo table with real water metrics
- Output path is relative: presentation/presentation_v2.pptx
</success_criteria>

<output>
After completion, create `.planning/phases/01-narrative-structure/01-01-SUMMARY.md`
</output>
