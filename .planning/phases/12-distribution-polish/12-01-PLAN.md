---
phase: 12-distribution-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - docs/quickstart.md
  - README.md
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "pyproject.toml [project.urls] references https://github.com/mott91/mace_gaussian_interface (not yourusername)"
    - "CI workflow installs ruff>=0.9.0 (not ==0.15.1), matching pyproject.toml dev dep floor"
    - "docs/quickstart.md shows mace-gaussian <subcommand> everywhere cli.py was referenced"
    - "README.md shows mace-gaussian <subcommand> everywhere cli.py was referenced (run_analysis.py references unchanged)"
    - "CLAUDE.md Commands section shows mace-gaussian for all cli.py invocations"
  artifacts:
    - path: "pyproject.toml"
      provides: "Real GitHub URLs in [project.urls]"
      contains: "mott91/mace_gaussian_interface"
    - path: ".github/workflows/ci.yml"
      provides: "Aligned ruff version constraint"
      contains: "ruff>=0.9.0"
    - path: "docs/quickstart.md"
      provides: "Correct entry-point in user-facing quickstart"
      contains: "mace-gaussian run"
    - path: "README.md"
      provides: "Correct entry-point in user-facing README"
      contains: "mace-gaussian run"
    - path: "CLAUDE.md"
      provides: "Correct entry-point in developer docs"
      contains: "mace-gaussian diagnose"
  key_links:
    - from: "pyproject.toml [project.urls]"
      to: "https://github.com/mott91/mace_gaussian_interface"
      via: "string replacement of yourusername placeholder"
      pattern: "mott91/mace_gaussian_interface"
    - from: "docs/quickstart.md"
      to: "mace-gaussian entry point"
      via: "replace all python cli.py occurrences"
      pattern: "mace-gaussian \\w"
---

<objective>
Close the four distribution-polish gaps from the v1.0 audit: replace placeholder GitHub URLs in
pyproject.toml with the real repository coordinates, align the CI ruff version pin with the dev
dependency floor, and update all user-facing and developer docs to show the installed `mace-gaussian`
entry point instead of `python cli.py`.

Purpose: Complete distribution-readiness so the package presents correctly to external researchers.
Output: Five edited files — zero placeholder URLs, no CI version skew, consistent entry-point docs.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pyproject.toml placeholder URLs and align CI ruff pin</name>
  <files>pyproject.toml, .github/workflows/ci.yml</files>
  <action>
In pyproject.toml, replace the three placeholder URL values in [project.urls]:

  Homepage = "https://github.com/yourusername/mace-gaussian-interface"
  Repository = "https://github.com/yourusername/mace-gaussian-interface.git"
  Issues = "https://github.com/yourusername/mace-gaussian-interface/issues"

Replace with:

  Homepage = "https://github.com/mott91/mace_gaussian_interface"
  Repository = "https://github.com/mott91/mace_gaussian_interface.git"
  Issues = "https://github.com/mott91/mace_gaussian_interface/issues"

CRITICAL: Do NOT touch the [project] name field ("mace-gaussian-interface") — that is the PyPI
package name and must remain unchanged. Only edit lines 50-52 ([project.urls] block).

In .github/workflows/ci.yml, change line 18 from:

  run: pip install "ruff==0.15.1"

to:

  run: pip install "ruff>=0.9.0"

This aligns the CI pin with the floor declared in pyproject.toml dev-dependencies. The Phase 9-02
decision used an exact pin for reproducibility; the floor-match approach removes the maintenance
burden for a research project (the audit classified this LOW risk).
  </action>
  <verify>
    <automated>grep "yourusername" /home/mot/mace_gaussian/pyproject.toml; echo "URL check exit: $?" && grep "mott91/mace_gaussian_interface" /home/mot/mace_gaussian/pyproject.toml | wc -l && grep "ruff==" /home/mot/mace_gaussian/.github/workflows/ci.yml; echo "Exact pin check exit: $?"</automated>
  </verify>
  <done>
`grep yourusername pyproject.toml` returns no output. Three lines contain `mott91/mace_gaussian_interface`.
`grep "ruff==" .github/workflows/ci.yml` returns no output (exact pin gone, replaced with >=).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update user-facing docs to mace-gaussian entry point</name>
  <files>docs/quickstart.md, README.md</files>
  <action>
In docs/quickstart.md, replace all occurrences of `python cli.py` with `mace-gaussian`.
There are 5 occurrences across lines 13, 23, 35, 43, 111. Every `python cli.py` in this file
must become `mace-gaussian`.

In README.md, replace all occurrences of `python cli.py` with `mace-gaussian`.
There are 8 occurrences across lines 71, 96, 128, 131, 134, 142, 145. Every `python cli.py`
in this file must become `mace-gaussian`.

CRITICAL: Do NOT touch any `python run_analysis.py` or `python run_analysis_harmonic.py` lines.
Those are analysis scripts with no entry point equivalent — they must stay as-is.

After editing, the commands will read:
  mace-gaussian diagnose
  mace-gaussian run water.xyz
  mace-gaussian run water.xyz --skip-dft-baseline
  mace-gaussian run molecule.xyz --energy-calculators mace_mp --dipole-calculators espaloma
  mace-gaussian list
  mace-gaussian list water
  </action>
  <verify>
    <automated>grep -n "python cli.py" /home/mot/mace_gaussian/docs/quickstart.md /home/mot/mace_gaussian/README.md; echo "Remaining cli.py references: $?" && grep -c "run_analysis" /home/mot/mace_gaussian/README.md</automated>
  </verify>
  <done>
`grep "python cli.py" docs/quickstart.md README.md` returns no output (exit 1 = no matches).
`grep -c "run_analysis" README.md` returns a positive count (run_analysis references preserved).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md Commands section to mace-gaussian entry point</name>
  <files>CLAUDE.md</files>
  <action>
In CLAUDE.md, update the ## Commands section (lines 11-27). Replace the 5 occurrences of
`python cli.py` with `mace-gaussian`:

  python cli.py diagnose                    # Check environment
  →  mace-gaussian diagnose                    # Check environment

  python cli.py run water.xyz               # Full workflow (recommended)
  →  mace-gaussian run water.xyz               # Full workflow (recommended)

  python cli.py run water.xyz --skip-dft-baseline
  →  mace-gaussian run water.xyz --skip-dft-baseline

  python cli.py run water.xyz --energy-calculators mace_mp --dipole-calculators espaloma
  →  mace-gaussian run water.xyz --energy-calculators mace_mp --dipole-calculators espaloma

  python cli.py list                        # List all results
  →  mace-gaussian list                        # List all results

The `python run_analysis.py water` and `python run_analysis_harmonic.py water` lines in the
## Commands section are intentional analysis scripts — do NOT change them.

Only replace `python cli.py` occurrences; leave all other content in CLAUDE.md untouched.
  </action>
  <verify>
    <automated>grep -n "python cli.py" /home/mot/mace_gaussian/CLAUDE.md; echo "Remaining cli.py in CLAUDE.md: $?" && grep -c "mace-gaussian" /home/mot/mace_gaussian/CLAUDE.md</automated>
  </verify>
  <done>
`grep "python cli.py" CLAUDE.md` returns no output (exit 1 = no matches, zero remaining occurrences).
`grep -c "mace-gaussian" CLAUDE.md` returns 5 (all five former cli.py invocations now use entry point).
  </done>
</task>

</tasks>

<verification>
Run all three checks together:

```bash
# No placeholder URLs
grep "yourusername" /home/mot/mace_gaussian/pyproject.toml && echo "FAIL: placeholder URL remains" || echo "PASS: URLs updated"

# Real URLs present (3 lines)
grep -c "mott91/mace_gaussian_interface" /home/mot/mace_gaussian/pyproject.toml

# No exact ruff pin in CI
grep "ruff==" /home/mot/mace_gaussian/.github/workflows/ci.yml && echo "FAIL: exact pin remains" || echo "PASS: pin aligned"

# No python cli.py in user docs
grep "python cli.py" /home/mot/mace_gaussian/docs/quickstart.md /home/mot/mace_gaussian/README.md /home/mot/mace_gaussian/CLAUDE.md && echo "FAIL: cli.py references remain" || echo "PASS: entry point updated"

# run_analysis.py references preserved in README
grep "run_analysis" /home/mot/mace_gaussian/README.md
```
</verification>

<success_criteria>
1. `grep "yourusername" pyproject.toml` — no output (zero placeholder URLs)
2. `grep "mott91/mace_gaussian_interface" pyproject.toml` — 3 lines (Homepage, Repository, Issues)
3. `grep "ruff==" .github/workflows/ci.yml` — no output (exact pin removed)
4. `grep "python cli.py" docs/quickstart.md README.md CLAUDE.md` — no output (all 18 occurrences replaced)
5. `grep "run_analysis" README.md` — positive output (analysis script references preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/12-distribution-polish/12-01-SUMMARY.md`
</output>
