---
phase: 03-extract-utilities-conventions
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - utils/results.py
  - utils/__init__.py
  - gm_main.py
  - dft_baseline.py
  - tests/test_regression.py
  - tests/test_units.py
  - pyproject.toml
  - docs/CONVENTIONS.md
autonomous: true
requirements: [STRUCT-01]

must_haves:
  truths:
    - "ResultsManager is importable from utils.results"
    - "Unit conversion functions in utils/units.py have tests verifying CODATA 2018 values and roundtrip accuracy"
    - "Code conventions are documented covering naming, error handling, units, and imports"
    - "pyproject.toml config reflects the new utils/ package (isort, coverage)"
  artifacts:
    - path: "utils/results.py"
      provides: "ResultsManager class"
      contains: "class ResultsManager"
    - path: "tests/test_units.py"
      provides: "Tests for unit conversion constants and functions"
      contains: "test_roundtrip"
    - path: "docs/CONVENTIONS.md"
      provides: "Documented code conventions"
      contains: "## Units"
  key_links:
    - from: "utils/results.py"
      to: "utils/validation.py"
      via: "lazy import for version metadata"
      pattern: "from utils\\.validation import collect_version_metadata"
    - from: "gm_main.py"
      to: "utils/results.py"
      via: "top-level import"
      pattern: "from utils\\.results import ResultsManager"
    - from: "dft_baseline.py"
      to: "utils/results.py"
      via: "top-level import"
      pattern: "from utils\\.results import ResultsManager"
---

<objective>
Move ResultsManager to utils/results.py, add unit conversion tests, update pyproject.toml config, and document code conventions.

Purpose: Completes the utils/ package extraction and establishes conventions that later phases (4-7) will follow.
Output: utils/results.py, tests/test_units.py, docs/CONVENTIONS.md, updated pyproject.toml.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extract-utilities-conventions/03-RESEARCH.md
@.planning/phases/03-extract-utilities-conventions/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move ResultsManager to utils/results.py, update imports and config</name>
  <files>utils/results.py, utils/__init__.py, gm_main.py, dft_baseline.py, tests/test_regression.py, pyproject.toml</files>
  <action>
1. Move `results_manager.py` to `utils/results.py`. Update the two lazy imports inside the class:
   - Line 139: `from validation import collect_version_metadata` -> `from utils.validation import collect_version_metadata`
   - Line 233: `from validation import collect_version_metadata` -> `from utils.validation import collect_version_metadata`
   Keep the try/except ImportError pattern — it's deliberate for resilience.

2. Delete old `results_manager.py`.

3. Update imports in consuming files:
   - `gm_main.py` line 30: `from results_manager import ResultsManager` -> `from utils.results import ResultsManager`
   - `dft_baseline.py` line 17: `from results_manager import ResultsManager` -> `from utils.results import ResultsManager`
   - `tests/test_regression.py` lines 245, 275, 305 (inside test methods): `from results_manager import ResultsManager` -> `from utils.results import ResultsManager`

4. Add `ResultsManager` re-export to `utils/__init__.py`: add `from utils.results import ResultsManager` to the existing re-exports.

5. Update `pyproject.toml`:
   - `[tool.ruff.lint.isort] known-first-party`: Remove `"exceptions"`, `"validation"`, `"results_manager"`. Add `"utils"`.
   - `[tool.coverage.run] source`: Remove `"results_manager"`. Add `"utils"`.
   - Keep all other entries unchanged.

6. Run `ruff check --fix && ruff format` to clean up any import ordering changes caused by the isort config update.
  </action>
  <verify>
Run `python -c "from utils.results import ResultsManager; rm = ResultsManager('/tmp/test_rm'); print('OK')"` — should print OK.
Run `python -c "import os; assert not os.path.exists('results_manager.py'); print('old file deleted')"`.
Run `pytest tests/test_regression.py -x -q` — regression tests pass with updated imports.
Run `ruff check pyproject.toml gm_main.py dft_baseline.py` — no errors.
  </verify>
  <done>ResultsManager lives in utils/results.py. Old results_manager.py deleted. All 5 import sites updated. pyproject.toml isort and coverage config updated for utils/ package.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit conversion tests and document code conventions</name>
  <files>tests/test_units.py, docs/CONVENTIONS.md</files>
  <action>
1. Create `tests/test_units.py` with tests for utils/units.py:
   - `TestConstants` class:
     - `test_bohr_to_angstrom_codata_2018`: assert `BOHR_TO_ANGSTROM == pytest.approx(0.529177210903, rel=1e-10)`
     - `test_angstrom_to_bohr_inverse`: assert `ANGSTROM_TO_BOHR == pytest.approx(1.0 / 0.529177210903, rel=1e-10)`
     - `test_hartree_to_ev_codata_2018`: assert `HARTREE_TO_EV == pytest.approx(27.211386245988, rel=1e-10)`
     - `test_ev_to_hartree_inverse`: assert `EV_TO_HARTREE == pytest.approx(1.0 / 27.211386245988, rel=1e-10)`
   - `TestConversionFunctions` class:
     - `test_hartree_to_ev`: assert `hartree_to_ev(1.0) == pytest.approx(27.211386245988, rel=1e-10)`
     - `test_ev_to_hartree`: assert `ev_to_hartree(27.211386245988) == pytest.approx(1.0, rel=1e-10)`
     - `test_bohr_to_angstrom`: assert `bohr_to_angstrom(1.0) == pytest.approx(0.529177210903, rel=1e-10)`
     - `test_angstrom_to_bohr`: assert `angstrom_to_bohr(1.0) == pytest.approx(1.8897259886, rel=1e-8)`
     - `test_roundtrip_energy`: `hartree_to_ev(ev_to_hartree(13.6))` approx 13.6 (rel=1e-12)
     - `test_roundtrip_distance`: `angstrom_to_bohr(bohr_to_angstrom(1.0))` approx 1.0 (rel=1e-12)
   - Follow existing test patterns: import pytest, use class-based grouping, descriptive method names.

2. Create `docs/CONVENTIONS.md` — pragmatic, reflecting actual codebase patterns:
   - **Naming**: snake_case functions/variables, PascalCase classes, UPPER_SNAKE_CASE constants, _leading_underscore for private
   - **Units**: Internal representation is eV (energy), Angstrom (distance), cm-1 (frequency), e*Bohr (dipole). All conversions via `utils.units` — never define constants inline.
   - **Error Handling**: Use exception hierarchy from `utils.exceptions`. Catch `MaceGaussianError` for broad handling, specific subclasses for targeted handling. Never bare `except:`.
   - **Imports**: stdlib -> third-party -> local (enforced by ruff isort). Local imports use `from utils.X import Y` for utils package.
   - **File Organization**: One class per module for large classes. Related pure functions grouped thematically. Tests mirror source: `utils/units.py` -> `tests/test_units.py`.
   - **Code Quality**: ruff for lint+format (line-length 100). Type hints on public function signatures. Docstrings on public functions (NumPy style).
   - Keep it under 80 lines. This is a reference, not a textbook.
  </action>
  <verify>
Run `pytest tests/test_units.py -v` — all tests pass.
Verify `docs/CONVENTIONS.md` exists and covers all 4 required areas (naming, error handling, units, imports): `head -50 docs/CONVENTIONS.md`.
  </verify>
  <done>Unit conversion tests verify CODATA 2018 values and roundtrip accuracy. Code conventions documented in docs/CONVENTIONS.md covering naming, error handling, units, imports, and file organization.</done>
</task>

</tasks>

<verification>
1. `pytest tests/ -x -q` — full test suite passes including new test_units.py
2. `python -c "from utils import ResultsManager, MaceGaussianError, BOHR_TO_ANGSTROM; print('all re-exports work')"` — package re-exports functional
3. `ruff check && ruff format --check` — clean codebase
4. No grep hits for `from results_manager import` in any .py file outside utils/
5. `ls results_manager.py 2>&1` — file should not exist
6. `docs/CONVENTIONS.md` exists with sections on naming, units, error handling, imports
</verification>

<success_criteria>
- ResultsManager importable from utils.results (and re-exported from utils)
- Old results_manager.py deleted
- pyproject.toml isort known-first-party includes "utils", excludes old module names
- pyproject.toml coverage source includes "utils", excludes "results_manager"
- tests/test_units.py has 10+ tests covering constants and roundtrip conversions
- docs/CONVENTIONS.md documents naming, error handling, units, imports conventions
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-extract-utilities-conventions/03-02-SUMMARY.md`
</output>
