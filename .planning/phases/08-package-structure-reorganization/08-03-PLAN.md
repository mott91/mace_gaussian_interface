---
phase: 08-package-structure-reorganization
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - pyproject.toml
  - tests/test_gaussian_parser.py
  - tests/test_fchk_parser.py
  - tests/test_calculators.py
  - tests/test_mace_loader.py
  - tests/test_cli_validation.py
  - tests/test_mode_matching.py
  - tests/test_exceptions.py
  - tests/test_units.py
  - tests/test_validation.py
  - tests/test_regression.py
  - tests/conftest.py
autonomous: true
requirements:
  - STRUCT-08
  - STRUCT-09
  - STRUCT-10

must_haves:
  truths:
    - "pyproject.toml entry point mace-gaussian = 'mace_gaussian.cli:cli' (no broken gm-main/gm-diagnose)"
    - "pyproject.toml packages = ['mace_gaussian'] in hatchling wheel target"
    - "All 131 tests pass after import path updates to mace_gaussian.*"
    - "mace-gaussian --help works after uv sync"
    - "Coverage config points to mace_gaussian source"
  artifacts:
    - path: "pyproject.toml"
      provides: "Correct build config and entry points"
      contains: "mace-gaussian = \"mace_gaussian.cli:cli\""
    - path: "pyproject.toml"
      provides: "Correct hatchling packages config"
      contains: "packages = [\"mace_gaussian\"]"
    - path: "tests/test_gaussian_parser.py"
      provides: "Updated imports"
      contains: "from mace_gaussian.gaussian.parser import"
    - path: "tests/test_cli_validation.py"
      provides: "Updated imports"
      contains: "from mace_gaussian.cli import cli"
  key_links:
    - from: "pyproject.toml"
      to: "mace_gaussian/cli.py"
      via: "project.scripts entry point"
      pattern: "mace_gaussian\\.cli:cli"
    - from: "tests/"
      to: "mace_gaussian.*"
      via: "updated absolute imports"
      pattern: "from mace_gaussian\\."
---

<objective>
Update pyproject.toml for correct build configuration and entry points, update all test imports to use mace_gaussian.* paths, run uv sync to reinstall the package, and verify the full test suite passes.

Purpose: This is the final "wiring" plan — making the package installable and all tests green. Without this plan the refactoring is incomplete and the project cannot be built or tested correctly.
Output: Working installable mace_gaussian package with passing test suite and correct pyproject.toml.
</objective>

<execution_context>
@/home/mot/.claude/get-shit-done/workflows/execute-plan.md
@/home/mot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-package-structure-reorganization/08-RESEARCH.md
@.planning/phases/08-package-structure-reorganization/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pyproject.toml and reinstall package</name>
  <files>
    pyproject.toml
  </files>
  <action>
Make ALL of these changes to pyproject.toml in one pass:

**1. Fix [project.scripts] — remove broken entry points, add correct one:**
```toml
[project.scripts]
mace-gaussian = "mace_gaussian.cli:cli"
```
Remove the stale `gm-main = "gm_main:main"` and `gm-diagnose = "gm_main:diagnose_main"` lines entirely.

**2. Fix [tool.hatch.build.targets.wheel] packages:**
```toml
[tool.hatch.build.targets.wheel]
packages = ["mace_gaussian"]
```
Replace `packages = ["."]` with the above.

**3. Fix [tool.ruff.lint.isort] known-first-party:**
```toml
[tool.ruff.lint.isort]
known-first-party = ["mace_gaussian"]
```
Remove stale entries: `calculators`, `gm_main`, `gaussian`, `analyze_spectra`, `comparison_workflow`, `mode_matching`, `utils`.

**4. Fix [tool.coverage.run] source:**
```toml
[tool.coverage.run]
source = ["mace_gaussian"]
omit = [
    "tests/*",
    "mace_dipole_pkg/*",
    "mace_ML_pkg/*",
]
```
Replace the old multi-module source list with just `"mace_gaussian"`.

After editing pyproject.toml, reinstall the package in editable mode:
```bash
cd /home/mot/mace_gaussian && uv sync
```

Then verify the entry point works:
```bash
cd /home/mot/mace_gaussian && mace-gaussian --help
```
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian && uv sync 2>&1 | tail -5 && mace-gaussian --help 2>&1 | head -5</automated>
    <manual>Confirm `mace-gaussian --help` prints the CLI help text without errors. Confirm pyproject.toml no longer contains gm-main or packages = ["."].</manual>
  </verify>
  <done>pyproject.toml updated with correct packages, entry point, isort, and coverage config. `uv sync` succeeds. `mace-gaussian --help` works.</done>
</task>

<task type="auto">
  <name>Task 2: Update all test imports to mace_gaussian.* and verify full suite passes</name>
  <files>
    tests/test_gaussian_parser.py
    tests/test_fchk_parser.py
    tests/test_calculators.py
    tests/test_mace_loader.py
    tests/test_cli_validation.py
    tests/test_mode_matching.py
    tests/test_exceptions.py
    tests/test_units.py
    tests/test_validation.py
    tests/test_regression.py
    tests/conftest.py
  </files>
  <action>
Update every test file import that references old flat module paths. Apply these replacements:

**tests/test_gaussian_parser.py:**
- `from gaussian.parser import GaussianLogParser` → `from mace_gaussian.gaussian.parser import GaussianLogParser`
- `from utils.exceptions import GaussianParseError` → `from mace_gaussian.utils.exceptions import GaussianParseError`

**tests/test_fchk_parser.py:**
- `from gaussian.fchk import extract_modes_from_fchk, parse_fchk_section` → `from mace_gaussian.gaussian.fchk import extract_modes_from_fchk, parse_fchk_section`

**tests/test_calculators.py:**
- `from calculators.base import DipoleCalculatorBase` → `from mace_gaussian.calculators.base import DipoleCalculatorBase`
- `from calculators.espaloma import EspalomaDipoleCalculator` → `from mace_gaussian.calculators.espaloma import EspalomaDipoleCalculator`
- `from calculators.mace_ml import MACEMLDipoleCalculator` → `from mace_gaussian.calculators.mace_ml import MACEMLDipoleCalculator`
- `from calculators.xtb import XTBDipoleCalculator` → `from mace_gaussian.calculators.xtb import XTBDipoleCalculator`
- Also update any pre-mock `sys.modules` entries if they reference `calculators`, `gaussian`, or `utils` by their old paths. The mock keys should remain as the ORIGINAL module names (e.g., `"mace.modules.models"`) which are external — those don't change. But any entries like `sys.modules["calculators"]` should become `sys.modules["mace_gaussian.calculators"]` if they exist.

**tests/test_mace_loader.py:**
- `from calculators.mace_loader import (...)` → `from mace_gaussian.calculators.mace_loader import (...)`
- Update any sys.modules pre-mocking that references `calculators` as a top-level module key.

**tests/test_cli_validation.py:**
- `from cli import cli` → `from mace_gaussian.cli import cli`
- Check for any other imports from `utils.*` or `gaussian.*` and update to `mace_gaussian.*`.

**tests/test_mode_matching.py:**
- `from mode_matching import (...)` → `from mace_gaussian.analysis.mode_matching import (...)`

**tests/test_exceptions.py:**
- `from utils.exceptions import (...)` → `from mace_gaussian.utils.exceptions import (...)`

**tests/test_units.py:**
- `from utils.units import (...)` → `from mace_gaussian.utils.units import (...)`

**tests/test_validation.py:**
- `from utils.exceptions import InputValidationError, PrerequisiteError` → `from mace_gaussian.utils.exceptions import ...`
- `from utils.validation import (...)` → `from mace_gaussian.utils.validation import (...)`

**tests/test_regression.py:**
- Read the current imports and update any that reference old flat module paths. Common patterns: `from gaussian.*`, `from utils.*`, `from mode_matching`.

**tests/conftest.py:**
- Check for any imports that reference old flat module paths and update.

After all edits, run the full test suite:
```bash
cd /home/mot/mace_gaussian && python -m pytest tests/ -x -q 2>&1 | tail -20
```

If tests fail due to sys.modules pre-mocking in test_calculators.py or test_mace_loader.py, update the mock hierarchy keys. The pre-mock pattern adds entries for heavy deps (DGL, espaloma, etc.) using their actual external module names — those don't change. Only entries that reference internal package names (calculators, gaussian, utils as top-level) need updating.

Run ruff on tests/ after all changes:
```bash
cd /home/mot/mace_gaussian && ruff check tests/ --fix && ruff format tests/
```
  </action>
  <verify>
    <automated>cd /home/mot/mace_gaussian && python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
    <manual>Confirm all tests pass (or the same set that passed before Phase 8 — no new failures introduced). Confirm `python -c "import mace_gaussian; print(mace_gaussian.__version__)"` prints 0.2.0.</manual>
  </verify>
  <done>All existing tests pass with updated mace_gaussian.* import paths. No new test failures introduced. Full suite green.</done>
</task>

</tasks>

<verification>
Phase 8 complete verification:
1. `python -c "import mace_gaussian; print(mace_gaussian.__version__)"` → `0.2.0`
2. `python -c "from mace_gaussian import run_pipeline; print('OK')"` → `OK`
3. `mace-gaussian --help` → prints CLI help without errors
4. `python -m pytest tests/ -x -q` → all tests pass
5. `python -c "from mace_gaussian.analysis import analyze_molecule; print('OK')"` → `OK`
6. `ruff check mace_gaussian/ tests/` → zero errors
7. `grep "packages" pyproject.toml` → contains `packages = ["mace_gaussian"]`
8. `grep "mace-gaussian" pyproject.toml` → contains `mace_gaussian.cli:cli`
9. `ls calculators/ gaussian/ utils/ cli.py workflow.py 2>/dev/null || echo "all moved"` → "all moved" (nothing at root)
</verification>

<success_criteria>
- pyproject.toml correctly configured: packages = ["mace_gaussian"], entry point = "mace_gaussian.cli:cli", no stale gm-main/gm-diagnose, correct isort and coverage source
- uv sync succeeds and installs the package from mace_gaussian/
- mace-gaussian --help works as a console script
- All tests pass with updated import paths
- STRUCT-08, STRUCT-09, STRUCT-10 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-package-structure-reorganization/08-03-SUMMARY.md`
</output>
